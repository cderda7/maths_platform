import SmartBoard from "./SmartBoard";

/** The smartboard: the projector's surface, display only. See `lib/board.ts` for what it shows per stage. */
export default function Page() {
  return <SmartBoard />;
}
