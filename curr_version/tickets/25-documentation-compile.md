# 25: Documentation compile

**What to build:** The finished state is documented. `ARCHITECTURE.md` at the repo root folds in the notes from tickets 17–24 with the system diagram updated for the second store, the new stages and the board route. `DECISION_LOG.md` gains entries for the two-store split, the pathway rule model, the detective feedback and guard, the universal grace, and the two-view board with marks mirrored to the student. `FUTURE_FEATURES.md` gets a final pass so every idea deferred during the build is present. Root and `curr_version` READMEs are recompiled for the finished state, including the demo script for the two-tab whole-class lesson and the `?pathway=` deep links.

**Blocked by:** 24 (Student freeze, marked view and session end).

**Status:** done

- [x] `ARCHITECTURE.md` system diagram and per-ticket sections updated for 17–24
- [x] `DECISION_LOG.md` entries written in the existing format (decision, context, alternatives, tradeoffs, defence)
- [x] `FUTURE_FEATURES.md` final pass
- [x] Root `README.md` and `curr_version/README.md` recompiled with the new demo script and deep links
- [x] Project `CLAUDE.md` still accurate
