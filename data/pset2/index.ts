import type { FinishedSet } from "../finishedSet";
import { PS2_ASSIGNMENT, PS2_PATHWAY } from "./assignment";
import { PS2_CLASSMATES, PS2_SAM } from "./classmates";
import { PS2_EVALUATION } from "./evaluation";

/** Problem Set 2 — Rationalising and expanding with surds (ticket 212), as a finished set. */
export const PS2: FinishedSet = {
  fixture: PS2_ASSIGNMENT,
  name: "Problem Set 2 — Rationalising and expanding with surds",
  pathway: PS2_PATHWAY,
  sam: PS2_SAM,
  classmates: PS2_CLASSMATES,
  evaluation: PS2_EVALUATION,
};
