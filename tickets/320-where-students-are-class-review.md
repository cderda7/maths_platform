# 320: Where students are during class review, minimal rows per example

**What to build:** the split from ticket 315 during class review, kept for a consistent look: on the left, a simple row per example the teacher picked, with minimal UI; on the right, the class review's own content.

**Blocked by:** 319.

**Status:** needs-design

**Triage:** `needs-design`: before building, the orchestrating session draws the screen and settles it with Carson.

---

## Problem Statement

The user (2026-09-15): "class review still have split just for consistent visual experience, do rows per example but have it be minimal UI, like simple". Class review is whole-class: the teacher works the examples picked in setup (`app/teacher/whole-class/`), with screens frozen or write with me.

## Open before building

- What a student pill means in a whole-class row (who wrote the example? who is writing with the teacher?)
- The right column during class review
- How it sits with the whole-class page's own controls

## Acceptance

Written once the design is settled, in the shape of ticket 315's.
