# 338: Past sets' review outcomes follow the after-individual-review rule

## Files touched

| File | What it does |
| --- | --- |
| `lib/reviewRule.ts` | `AFTER_REVIEW_RULE_SETS` and `afterReviewRule` are gone. `afterIndividual(n)` reads the set's pathway from the story sheet (every set has individual review). `reviewByRule` runs ticket 332's rule on every set: helpers by `canExplain`, a group call only on its union (`stillToReview`), "fixed on own rework" from the second submission. Ticket 281's first-submission branch is removed. |
| `lib/classStory.ts` | `reviewMismatches` checks every set with ticket 332's rule: a one-off has a second submission (holding for individual, wrong otherwise), no other basis has one, and a group version sits exactly where the question is in its group's union. A second submission on a pathway without individual review is a mismatch. Second-submission lines are checked against the set's table even when no group version follows. The sheet's rules bullet states one rule. |
| `data/pset1..5/review.ts` | Group versions only on each group's union after individual review; amber has none on Sets 1 and 2 (it sat out). Seven rewrites slip again (below). PS5's `MINT_Q9` and `SKY_Q4` last tries are gone (both now solved). |
| `data/story.ts` | `STORY_REVIEW` Sets 1–5: 64 rows re-reasoned. Group rows name who explained (right first time, and who fixed it in individual review). Still-wrong rows say whose rewrite slipped again. PS5's three newly solved rows are rewritten. The doc comment and the hardest-problem list are updated. |
| `specs/class-story.md` | Regenerated. |
| `data/pastSetsReview.test.ts` (new) | Covers Sets 1–5: group versions equal the union, amber sits out Sets 1 and 2, a sat-out student's report has no group version, the outcome counts next to the old ones, one question per set nobody could explain, the seven rewrites that slip again, and set scores pinned. |
| `data/story.test.ts`, `lib/report.test.ts` | Set 5's hardest list is mint's Q10 only. Grace's Set 5 report reads Q8 and Q9 in group review and Q10 incorrect. |

## Outcome counts per set (own rework / group / still wrong)

| Set | Before (ticket 278 rule) | After (ticket 332 rule) | Why |
| --- | --- | --- | --- |
| PS1 | 13 / 12 / 3 | 12 / 12 / 4 | Oliver fixed Q10, which would have let violet solve it, the set's only question nobody could do. His rewrite now slips again (the side left as 2√18), so his Q10 moves from own rework to still wrong. Amber sits out: Chloe fixed Q1 and Q5 and nobody else had anything left. |
| PS2 | 25 / 18 / 2 | 23 / 18 / 4 | Isla and Lucas fixed Q10, which would have made them mint's helpers on its only unsolved question. Both rewrites now slip again (Isla's sentence answers only the area; Lucas's (3 − √2)² middle term is not doubled). Amber sits out: Noah's Q3, Chloe's and Ethan's Q8, Mia's and Chloe's Q9 were all one-offs they fixed. |
| PS3 | 22 / 28 / 2 | 20 / 28 / 4 | Same shape at mint Q10. Isla's rewrite leaves the last sign in the bracket unchanged, and Lucas's collects x² − x² as 2x². Class review still covers mint's Q10 with Isla's and Lucas's first submissions. |
| PS4 | 25 / 45 / 3 | 24 / 45 / 4 | Lucas fixed Q10, which would have made him mint's helper. His rewrite now keeps the negative width beside the answer. |
| PS5 | 29 / 28 / 6 | 28 / 31 / 4 | Sky's Q4 is now solved: Sam and Zara fixed it and explain it, so Jordan and Liam move from still wrong to group. Mint's Q9 is now solved: Isla, Lucas and Harper fixed it, so Grace moves from still wrong to group. Harper fixed Q10, which would have made mint solve its last question, so her rewrite now slips again (the working written out, the height still 2 m). |
| PS6 | 16 / 41 / 15 (ticket 332) | 16 / 41 / 15 | Already under the rule; unchanged. |

Group questions solved / unsolved per set, after: PS1 12 / 1, PS2 18 / 1, PS3 18 / 1, PS4 30 / 1, PS5 24 / 1 (amber works nothing on PS1 and PS2).

## Second submissions changed (first submissions untouched, set scores unchanged)

| Set | Student | Question | The rewrite now | Why |
| --- | --- | --- | --- | --- |
| PS1 | Oliver | Q10 | diagonal right, side left as 2√18 (square factor left in root) | the only question nobody at violet could do |
| PS2 | Isla | Q10 | working right, the sentence answers only the area | mint's Q10 needs no helper at the table |
| PS2 | Lucas | Q10 | both squares written out, the second's middle term not doubled | the same |
| PS3 | Isla | Q10 | bracket written out, its last sign left unchanged | mint's Q10, and the question class review covers |
| PS3 | Lucas | Q10 | every sign changed, x² − x² collected as 2x² | the same |
| PS4 | Lucas | Q10 | width named right, the negative width kept beside it | mint's Q10 |
| PS5 | Harper | Q10 | working written out, the height still given as 2 m | mint's Q10, the set's last question nobody could do |

Each new line is already in its set's evaluation table, and each rewrite is a slip another student made on that question, never a copy of anyone's first submission.

## How it connects

```
 data/psetN/classmates.ts ─ first submissions (done, wrong, attempts)   (unchanged: set scores unchanged)
          │
          │ withReview(records, PSn_REVIEW)            data/psetN/review.ts
          │◀──────────────────────────────────────────  second: one-off rewrites (7 now slip again)
          ▼                                             groups: versions on each group's union only
 Classmate.review { second, group }                            (amber: none on PS1, PS2)
          │
          ├──────────────▶ lib/report.ts recordReviews / outcomeOf / columnsOf / shownVersions
          │                        │
          │                        ├──▶ app/teacher/report/TeacherReport.tsx (every student, every set)
          │                        └──▶ lib/studentReport.ts ──▶ Sam's Completed-set report
          │
          ▼
 lib/reviewUnion.ts  recordWork ─▶ stillToReview / canExplain / fixedInIndividualReview
          │
          ▼
 lib/reviewRule.ts  afterIndividual(n) = STORY_SETS[n-1].pathway has "individual"
          │  reviewByRule: helpers = canExplain; group call only on the union; own = fixed in individual review
          ▼
 lib/classStory.ts  reviewMismatches / hardestUnsolved / classReviewMismatches  (every set, one rule)
          ▲                                   │
          │                                   ▼
 data/story.ts STORY_REVIEW (Sets 1–6) ──▶ specs/class-story.md (npm run story:sheet)
          ▲
 data/finishedSets.test.ts, data/story.test.ts, data/pastSetsReview.test.ts
```
