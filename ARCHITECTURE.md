# Architecture — Edexia · Maths

Running architecture record for the closed-loop demo. One row per completed ticket, in build
order (01–16 spec v2, 17–25 spec v3); per-ticket detail lives in `architecture/<nn>-<slug>.md`. Paths below are
relative to the repo root. Next.js 16 App Router, React 19, Tailwind 4, KaTeX; no backend beyond
two routes, the help chat's (`/api/help-chat`, ticket 69) and problem extraction (`/api/extract`, ticket 170), all data static under
`data/` (plus the rendered extraction fixtures under `fixtures/extract/`). The Sept 7 mockup was removed on 10 Sep 2026 (ticket 60).

## System diagram

```
 browser tab A · student iPad                        browser tab B · teacher laptop
 ┌──────────────────────────────────┐                ┌──────────────────────────────────┐
 │ /student  StudentClassroom (264) │               │ /teacher  page.tsx ▶ TeacherLive │
 │                                  │               │ presenter strip: SKIP TO (263) · Reset demo → setLesson│
 │   └▶ Completed card ▶ /student/a/<id>/report CompletedReport (287) ▶ screens/ReportLayout ◀ lib/studentReport│
 │   └▶ Completed ▶ homework column beside the cards (290) ◀ lib/homeworks (date rule, status, row spans) ◀ data/homeworks · components/CautionTriangle (shared with TeacherLive Missing)│
 │   └▶ /teacher +Homework (291) ▶ /teacher/homework/create (CreateAssignment kind=homework) ▶ …/review Create ▶ homework/send ▶ classroom.homeworks ◀ lib/homeworks classHomeworks · homework column beside Past (305) ◀ lib/classroomCards teacherHomeworkColumn · lib/homeworks homeworkDoneCount ◀ data/homeworks CLASS_HOMEWORK_STORY│
 │ /teacher/assignments/create      │                │   (no board indicator, ticket 55)│
 │   CreateAssignment ▶ QuestionTile (the tile is the editor) ▶ QuestionView ◀ lib/mathInput (x**2 → KaTeX; $…$ inline; TeX as written); draft/set ▶ …/create/review (stub)│
 │     drop · ⌘V · Upload (tickets 171–172: pictures and PDFs) ▶ lib/upload partitionDrop ▶ lib/sources (IndexedDB) · lib/pdfPages (pdfjs: page count, page thumbnails) ▶ lib/extractClient ▶ POST /api/extract ▶ PendingTile → unconfirmed tiles (✓ ×, "4(a) · p. 2", a figure cut by lib/crops) · MessageTile; the bar: Discard N · Add N · Continue│
 │     typed text on Enter / blur ▶ the same route (ticket 173) ▶ item.model (the parser's preview until it lands) · a Fix line on every focused tile ▶ { mode: "fix" } ▶ the tile's text replaced│
 │ /teacher/assignments/new (old)   │
 │   NewAssignment ▶ PathwayMap     │
 │ /teacher/whole-class ▶ setup     │
 │ /teacher/board ▶ BoardControls   │   (pad · prev · mode · marks · End · next; no examples)
 │   └▶ /student/a/pset-6 StudentApp│                │   useBatchedSession(3 s)         │
 │   ForceSubmit → advance/start    │
 │       useStudentSession()        │                │   subskillStatuses · caution     │
 │       └▶ IpadStage ▶ StudentChrome│               │   classmates (19: 6 full, 13 light) · seating groups (5 colours)│
 │            └▶ screens/            │               └──────────────┬───────────────────┘
 │               Overview ▶ Confidence ▶ (not confident: a callout, offerLines: Warm up | Start the set)│                              │ reads every 3 s
 │               ▶ WarmupChat (the ticked skills → concernTurns, the skill in a light blue box (ink text) in each ask, later ones "How about…?"; one bubble at a time to turnSteps, the box off while the tutor "writes" → answers → focus → warmupSequence, easiest first)│
 │               ▶ Practice (pad · skill buttons: dark once on or through, tap opens · HelpMenu: hint · worked example · video · chat · follow-up split pane)│
 │                   HelpChat (ticket 69): under the read-as lines while open, content-sized to a 42% cap then scrolling (ticket 103) · lines said → run.chat · POST /api/help-chat streams claude-opus-5 (lib/helpChat.ts brief: hints only, two ways in, "which makes more sense?")│
 │                   HintCard: linked hint words light the expression (termTex) — practices only│
 │               ▶ Working ─▶ DrawPad (canvas ink)                  │
 │                                  ├▶ "Read as" column             │
 │                                  └▶ PromptModal · HelpPicker (this problem's moves) · PracticeOverlay = PracticePad (shared with the warm-up)
 │               ▶ Feedback = individual review (detective sentence · star · what you submitted · pad · guard · hand in) · Waiting│
 │               ▶ Frozen (versions beside a pad: teacher-ink mirror or write-with-me; marks follow the board)│
 │               ▶ ClassWait (gate: n of 20 · teacher start) ▶ GroupBoard (one shared whiteboard · the pen by shuffle · check) ▶ GroupDebrief (three versions · a note · marks · 20 s hold)              │
 │                 └─ both under one GroupHeader (problem · GroupBar centred in a 3-column grid · pen chip or "the group got it")                                        │
 │               ▶ Report (teacher's colours + reflection → sent)   │
 │               ▶ Homework (after Send: ever-wrong tiles turn into similar problems and fly into the folder, ticket 256)│
 │               ▶ Peers (mastery only: class struggles, counts)    │
 │               ▶ History (final only; compare scroll-synced)      │
 │               DiagnosticModal over any stage while a push is pending
 └───────────────┬──────────────────┘                               │
 browser tab C · smartboard (projector, display only)               │
 ┌──────────────────────────────────────────────────────────────┐   │
 │ /board ▶ SmartBoard  useClassroom · useLiveSession · useNow   │   │
 │   boardContent → blank (class · title) · holding (standings  │   │
 │   placeholder) · slide (examples A/B/C · n/m students · marks│   │
 │   iff marked · read-only mirror of the teacher's ink)        │   │
 │   no button, no link, no live pad, no name                   │   │
 └──────────────────────────────────────────────────────────────┘   │
                 │ dispatch(action)                                 │
                 ▼                                                  ▼
 ┌────────────────────────────────────────────────────────────────────────────────────┐
 │ lib/store.ts   one StudentSession · localStorage snapshot · BroadcastChannel        │
 │                useStudentSession · useBatchedSession · useNow · resetSession        │
 │                dispatch(action) runs the reducer under env { pathway } from …       │
 │ lib/classroom-store.ts  ClassroomState (teacher-owned, own key + channel)           │
 │                useClassroom · dispatchClassroom · resetClassroom                    │
 │ lib/classroom.ts    CreatedAssignment { title, problemIds, pathway } · pathwayOf    │
 │                     PendingAdvance { id, kind, deadline } · GRACE_MS · isPending/isDue│
 │                     WholeClassSession { problems, examples, slide, view, status }     │
 │ lib/examples.ts     candidatesFor · bucketOf · suggestExamples · boardExamples (no names)│
 │                     lineMarks (red / blue for the marked view, board and student alike)  │
 │ lib/frozen.ts       frozenView(session, classroom) → the student's own work on the slide │
 │ lib/board.ts        boardContent(classroom, session, now) → blank | group | holding | whole-class│
 │ lib/standings.ts    standingsAt (per seating group: live run or the scripted race) · rankStandings│
 │ lib/assignment.ts   activeAssignment(classroom) → created title + problems, or fixture│
 │ lib/pathway.ts      REVIEW_ORDER · successors · nextStage · pathwaySentence/Chip    │
 │ lib/session.ts      StudentSession · sessionReducer(s, a, env) · sessionAt (pure)   │
 │ lib/recognition.ts  nextLine · afterUndo  (burst of strokes → scripted line)        │
 │ lib/hint.ts         hintSegments · findFragment · termTex (\htmlClass wraps, no layout change)│
 │ lib/helpChat.ts     findPractice · parseHelpChatRequest · helpChatSystem (the tutor's brief) · helpChatMessages · chatSegments│
 │   app/api/help-chat/route.ts  a live model call: Anthropic SDK stream → text/plain; 503 with no credentials│
 │ lib/extract.ts      Source · caps · parseExtractRequest · pdfPageCount · extractSystem (the extractor's brief) · extractMessages (image / native PDF blocks) · parseDraftLine · readExtractEvents · sourceHash│
 │ lib/upload.ts       partitionDrop · dropNote · Item (question | pending | message) · insertBefore/updateQuestion/replaceItem/removeItem · confirmAll · discardUnconfirmed · draftItem · failureMessage│
 │ lib/sources.ts      putSource · getSource · deleteSource (IndexedDB, a Map without it) · thumbOf (160 px JPEG data URL) · thumbSize│
 │ lib/extractClient.ts fileToSource (image or pdf) · extractSource (one POST, events as they arrive; ExtractError with the route's failure) · bytesToBase64│
 │ lib/pdfPages.ts     openPdf · pageCount · renderPage · pageThumb (pdfjs-dist loaded on first use, its worker bundled by Next) · fitScale│
 │ lib/crops.ts        cropRect · cropFigure · cropFromImage (a figure box as a full PNG for the store and a ≤ 480 px JPEG for the tile)│
 │   app/api/extract/route.ts  POST { sources } → application/x-ndjson, one {"type":"draft"} line per problem as the model writes it, a "done" per source; EXTRACT_FIXTURES=1 answers from data/extract-fixtures.ts (fixtures/extract/manifest.json by hash, typed text via mathInput); 503 with no credentials│
 │   session.ink / reworkInk: strokes per problem, popped with lines on undo/clear      │
 │ lib/evaluate.ts     evaluateLine(problem, tex) → ok | wrong | unclear               │
 │ lib/stepCheck.ts    checkStep({ tex, tags }, line) → right | wrong (misconception) | unreadable: a blank step by form (311)│
 │   ◀ lib/texEval.ts  tokenizeTex · expressionAt → TexNode (one grammar; evalTex evaluates the same tree) ◀ lib/mathInput toTex│
 │ lib/warmup.ts       warmupScript(p, slips) = padScript(steps, slips): a demo's wrong lines before a step's own (311)│
 │ lib/escalation.ts   recordMistake · requestHelp → { trigger, cautioned }            │
 │ lib/hierarchy.ts    hierarchyFor(evidence) → leaf/group/category status · half dots  │
 │                     sessionEvidence · classmateEvidence (one path) · categoriesTouched │
 │ lib/unit.ts         inferUnitFromProblems · inferUnitFromText                        │
 │ lib/feedback.ts     runKind · feedbackFor (teacher views) · feedbackSummary (student) │
 │ lib/guard.ts        guardFor · trippedProblems  (originally-correct problem broken)  │
 │ lib/group.ts        computePhases (∩ correct / ∪ wrong) · groupPlan → DiscussionView │
 │ lib/report.ts       reportFacts · confidenceSentence  (same text on both sides)      │
 │ lib/commentary.ts   commentaryFor(student, session) → ideas + clarification (individual view) │
 │ lib/mistakes.ts     mistakesByProblem → problem → rows (live student + classmates)   │
 │ lib/place.ts        classPlaces · sessionPlace (Sam) · classmateTimeline (data/stream script) · placeRows (314)│
 │ lib/pairs.ts        blankSteps (the one blank-line rule) · pairFor · warmupSteps (310)                        │
 │ lib/peers.ts        peerStruggles (counts only) · isMastery                          │
 │ lib/versions.ts     versionsOf (handed in / after rework) · alignVersions            │
 │ lib/groups.ts       reviewGroups → per group: member lines + one shared note         │
 │ lib/diagnostic.ts   isCorrect                                                       │
 └───────────────────────────────────────┬────────────────────────────────────────────┘
                                         ▼ reads
 ┌────────────────────────────────────────────────────────────────────────────────────┐
 │ data/  (static TypeScript, no fetching)                                            │
 │   taxonomy.ts     TAXONOMY (7 categories → groups → leaves) · LeafId · lookups · version │
│   misconceptions.ts MISCONCEPTIONS (37, permanent ids → name, about) · MisconceptionId (299) │
 │   types.ts        Tag · Status (5 levels) · Problem · Assignment · UnitRef · Confidence · Stage │
 │   assignment.ts   ASSIGNMENT (10 problems, tagged solutions) · unitLabel · DEMO_STUDENT │
 │   recognition.ts  RECOGNITION[problemId] (scripted run) · RECOGNITION_REWORK (corrected) │
 │   evaluation.ts   EVALUATION[problemId][tex] → LineVerdict (ok/wrong, subskill, clue, misconception) │
 │                   STANDOUT[problemId][tex] → { when: strong|weak|both, why }              │
 │   practice.ts     PRACTICES[subskill]: one isolated practice problem each · PRACTICE     │
 │   pairs.ts        QUESTION_PAIRS (Q* worked, Q** finished, per set question) · COMPLETIONS[leaf] (310) │
 │   classmates.ts   CLASSMATES (rows, wrong sets, attempts, group lines) · groups          │
 │   race.ts         RACE_SCHEDULE: the other groups' finish moments, seconds from the start │
 │   diagnostic.ts   DIAGNOSTICS: the live multiple-choice question the teacher can push    │
 └────────────────────────────────────────────────────────────────────────────────────┘
                 ▲ reads (a chip needs only an id)
 ┌───────────────┴────────────────────────────────────────────────────────────────────┐
 │ components/  (presentational kit, no page deps)                                    │
 │   ui.tsx  Card Eyebrow H1 H2 Button Avatar    Math.tsx  M (katex.renderToString)   │
 │   Tag.tsx DifficultyTag LeafChip StatusDot(half) STATUS_WORD    Figure.tsx (Q8 svg) │
 │   Figure.tsx PARABOLAS: Q8's graph, its similar problem's, Q8* and Q8** (310)          │
 │   HierarchyDrill.tsx  RowDrill: trees per column → leaf → work, grid and reports  │
 │   Brand.tsx Brand BrandMark                   IpadStage.tsx  bezel + scale-to-fit  │
 │   DrawPad.tsx  pointer events → ink; reports pen-down and burst-end(strokeCount)   │
 │   InkView.tsx  read-only SVG of stored strokes, cropped and fitted to its box       │
 │   PadSection.tsx  pad + Undo/Clear      ReadAs.tsx  transcription column + shimmer  │
 │   PracticeCard.tsx  one practice problem, steps revealed one at a time              │
 │   PracticePad.tsx   practice on the pad (warm-up + mid-set): hint, example, follow-up│
 │   HintCard.tsx      a practice hint with linked words; reports the hovered term      │
 │   ResetDemo.tsx     restart the shared session in every tab                         │
 └────────────────────────────────────────────────────────────────────────────────────┘
 ┌────────────────────────────────────────────────────────────────────────────────────┐
 │ app/layout.tsx  fonts · katex.css · globals.css (@theme tokens, .ipad-bezel/.screen)│
 │ app/page.tsx    / chooser: student iPad, teacher view, smartboard, all three /split│
 │ app/demo/       /demo ─307─▶ / (the chooser's address in tickets 265–285)          │
 │ app/split/      SplitView: /student, /teacher, /board in scaled iframes, any of them  │
 │                 (lib/split.ts: panes, a design size each · placeFor · frameFor ·     │
 │                  dragStep: a divider drag ends the moment the button is not held)   │
 │ scripts/laptop-check.mjs  every teacher route at two laptop widths, no x-overflow    │
 └────────────────────────────────────────────────────────────────────────────────────┘

 Dependency rule: app ──▶ components ──▶ data ──▶ types. Nothing points the other way.
```

## The six-set Classroom (tickets 208–217, 234, 237, 251, 252, 253)

```
 data/story.ts  class story sheet: STORY_SETS (PS1–PS6) + STORY (20 students × 6 categories × 6 sets)
      │  ──► lib/classStory.ts renderClassStory ──► specs/class-story.md (npm run story:sheet)
      │  ──► data/story.test.ts (complete, one step, Set 6 == classmates' end state)
      │  ──► data/finishedSets.test.ts (every registered set == its column)
      ▼
 data/pset1/ … data/pset5/   assignment.ts · evaluation.ts · classmates.ts · index.ts (PSN: FinishedSet)
      │
      ▼
 data/finishedSets.ts  export { PS1 } … { PS5 }  ──► lib/finishedSets.ts FINISHED_SETS (oldest due first)
      │                                                   │                 │
      │   data/assignment.ts ASSIGNMENT (Set 6, live)      ▼                 ▼
      │          │                              lib/evaluate.ts        lib/seating.ts
      ▼          ▼                              tables by problem id   frozen groups per set
 lib/assignments.ts REGISTRY: pset-6 live, then pset-5 … pset-1
      │  assignmentBundle(id) { title, name, due, problems, newSkills, classmates, sam, groups }
      │
      ├─► app/teacher/Classroom.tsx   pinned: heading, Holistic Assessment + "+In-Class PSet", LIVE (Set 6); Past scrolls, newest due first
      ├─► /teacher/a/<id>/class       ClassView ─► TeacherLive: rows, dots, due line; HierarchyDrill
      │        lib/hierarchy.ts hierarchyFor(ev, set): a set's New skills → "new", others → their home
      ├─► /teacher/a/<id>/mistakes, /groups, /report?student=
      ├─► lib/setHistory.ts categoryHistory(id, student, category)
      │        earlierSources ─► the earlier finished sets that assessed the category, at most five, dated "MON 7 SEP"
      │        (none: no stack; the first set: no see history)
      │        └─► TeacherLive history pill: Link ─► /teacher/a/<id>/class?report=<earlier>&student=&open=
      │                 ClassView ─► EarlierReport: this set's chrome, the earlier set's ReportBody,
      │                 pulsing "← Return to PSet N" ─► ?history=<student>&open=<category>
      ├─► lib/holistic.ts holisticView(student, { classroom, session, now })   (ticket 251)
      │        finished sets ─► the sheet's cells and patterns; the live set ─► rosterEvidence (as its Class View reads it)
      │        surfacing(…, recentSets(sets)): a pattern shows when its tag has a set among the latest five, with all its sets   (ticket 276)
      │        └─► app/teacher/students/HolisticPage.tsx   /teacher/students/<id> (Back → Holistic Assessment)
      │                                                    /teacher/a/<set>/students/<id> (Back → that Class View)
      │                 set header ─► /report?student=&from=     pattern "PS4 · Q1" ─► /report?student=&work=&from=
      │                 ▲ TeacherLive: a name or either avatar ─► /teacher/a/<set>/students/<id>   (ticket 253)
      │                   "Did you know?" in the Student head ─ Dismiss ─► lib/holisticNote.ts (own localStorage key)
      │        └─► lib/holisticTiles.ts holisticTiles(…)   (ticket 252)   data/patternTags.ts: one label per pattern worded by set
      │                 the page's patterns as "label · n sets" ("· 1 set") by category; strengths: secure on every set that assessed them
      │                 └─► app/teacher/students/HolisticTiles.tsx   /teacher/students (Classroom's title-row entry)
      │                          tile ─► /teacher/students/<id> ; Back returns at the scroll left (tilesScroll.ts)
      └─► lib/newSkills.ts inferNewSkills(problems, recentSets("pset-6", 2) = PS5, PS4) ─► Create's review
               strip: lib/createPipeline.ts PIPELINES[kind] QUESTIONS — DIFFICULTY — REFINE — PATHWAY — SEND (ticket 288)
               due: components/DuePicker.tsx beside the title ─► draft.due ─► assignment.due (IsoDay) ─► bundle.due "Thu 10 Sep" ─► every card (ticket 289)
```

## Tickets, in build order

| # | Ticket | Routes | Commit | Note |
|---|---|---|---|---|
| 01 | Scaffold, iPad stage, demo assignment fixture | `/`, `/student`, `/teacher` | `bc49ffa` | [architecture/01-scaffold.md](architecture/01-scaffold.md) |
| 02 | Pre-assignment skill list, practice offer, confidence survey | `/student?stage=…` | `5ce1673` | [architecture/02-pre-assignment-and-confidence.md](architecture/02-pre-assignment-and-confidence.md) |
| 03 | Drawpad with simulated line-by-line recognition | `/student?stage=working` | `bcca326` | [architecture/03-drawpad-simulated-recognition.md](architecture/03-drawpad-simulated-recognition.md) |
| 04 | Scripted evaluation, escalation counter, practice prompt, "I need help" | `/student?stage=working` | `db5cbb1` | [architecture/04-scripted-evaluation-and-escalation.md](architecture/04-scripted-evaluation-and-escalation.md) |
| 05 | Teacher live subskill status and caution flag | `/teacher`, `/student` | `a816258` | [architecture/05-teacher-live-status-and-caution-flag.md](architecture/05-teacher-live-status-and-caution-flag.md) |
| 06 | Feedback layers on submission | `/student?stage=feedback` | `c09798e` | [architecture/06-feedback-layers.md](architecture/06-feedback-layers.md) |
| 07 | Independent rework stage | `/student?stage=rework` | `6dbf08f` | [architecture/07-independent-rework.md](architecture/07-independent-rework.md) |
| 08 | Simulated group review, two phases | `/student?stage=group-pass`, `…=group-discuss` | `8b2f174` | [architecture/08-simulated-group-review.md](architecture/08-simulated-group-review.md) |
| 09 | Student final report and reflection | `/student?stage=report` | `7c66e0a` | [architecture/09-student-final-report.md](architecture/09-student-final-report.md) |
| 10 | Teacher final report | `/teacher/report` | `d3fb1c9` | [architecture/10-teacher-final-report.md](architecture/10-teacher-final-report.md) |
| 11 | Teacher mistake view | `/teacher/mistakes` | `5ac30a1` | [architecture/11-teacher-mistake-view.md](architecture/11-teacher-mistake-view.md) |
| 12 | Peer-struggle screen (Tier 2) | `/student?stage=report&run=strong` → peers | `a53863a` | [architecture/12-peer-struggle-screen.md](architecture/12-peer-struggle-screen.md) |
| 13 | Submission history (Tier 2) | `/student?stage=history` | `67db35d` | [architecture/13-submission-history.md](architecture/13-submission-history.md) |
| 14 | Teacher review-groups view (Tier 2) | `/teacher/groups` | `0c268b0` | [architecture/14-teacher-review-groups-view.md](architecture/14-teacher-review-groups-view.md) |
| 15 | Teacher original vs final (Tier 2) | `/teacher/compare` | `3fb499b` | [architecture/15-teacher-original-vs-final.md](architecture/15-teacher-original-vs-final.md) |
| 16 | Diagnostic MCQ push (Tier 2) | `/teacher` → `/student` interrupt | `7b3d49c` | [architecture/16-diagnostic-mcq-push.md](architecture/16-diagnostic-mcq-push.md) |
| 17 | Copy sweep to the rule (spec v3) | every route | `cabb007` | [architecture/17-copy-sweep.md](architecture/17-copy-sweep.md) |
| 18 | Pathway model and routing | `/student?pathway=…`, `/teacher` chip | `dc5ee2a` | [architecture/18-pathway-model-and-routing.md](architecture/18-pathway-model-and-routing.md) |
| 19 | Assignment creation with the pathway map | `/teacher/assignments/new` | `2af6872` | [architecture/19-assignment-creation-with-pathway-map.md](architecture/19-assignment-creation-with-pathway-map.md) |
| 20 | Persisted ink | `/student` working, rework, history | `67aec66` | [architecture/20-persisted-ink.md](architecture/20-persisted-ink.md) |
| 21 | Detective feedback and the guard | `/student?stage=feedback`, rework | `3479206` | [architecture/21-detective-feedback-and-guard.md](architecture/21-detective-feedback-and-guard.md) |
| 22 | Teacher force submit with one-minute grace | `/teacher` Class card → `/student` pill | `bd16a06` | [architecture/22-teacher-force-submit-with-grace.md](architecture/22-teacher-force-submit-with-grace.md) |
| 23 | Whole-class setup and the unmarked board | `/teacher/whole-class`, `/teacher/board` | `bb83c90` | [architecture/23-whole-class-setup-and-unmarked-board.md](architecture/23-whole-class-setup-and-unmarked-board.md) |
| 24 | Student freeze, marked view and session end | `/student` frozen, `/teacher/board` marks, `/teacher` End | `9acced2` | [architecture/24-student-freeze-marked-view-and-session-end.md](architecture/24-student-freeze-marked-view-and-session-end.md) |
| 25 | Documentation compile | — | `4e7b2a1` | [architecture/25-documentation-compile.md](architecture/25-documentation-compile.md) |
| 26 | Hierarchical skill category dashboard | `/teacher` grid + drill, reports, creation Unit Focus | `b7f5a71` | [architecture/26-hierarchical-skill-dashboard.md](architecture/26-hierarchical-skill-dashboard.md) |
| 27 | Warm-up on the pad, confidence first, multimodal help | `/student?stage=confidence`, `…=practice` | `9f311a5` | [architecture/27-warm-up-on-the-pad.md](architecture/27-warm-up-on-the-pad.md) |
| 28 | Warm-up chooser: problems, words, one skill at a time | `/student?stage=warmup-pick`, `…=practice` | `c614ed5` | [architecture/28-warm-up-chooser.md](architecture/28-warm-up-chooser.md) |
| 29 | Mid-set isolated practice on the pad | `/student?stage=working` overlay | `2022a51` | [architecture/29-isolated-practice-on-the-pad.md](architecture/29-isolated-practice-on-the-pad.md) |
| 30 | Hint words that light the problem | `/student?stage=practice`, `…=working` overlay | `917bb70` | [architecture/30-hint-links.md](architecture/30-hint-links.md) |
| 31 | Practice sent to the fundamental skill; confidence for the teacher | `/student?stage=working` prompt | `18b97f0` | [architecture/31-confidence-triggered-practice.md](architecture/31-confidence-triggered-practice.md) |
| 32 | Individual review with correction on one screen | `/student?stage=feedback` | `66fa851` | [architecture/32-review-with-correction.md](architecture/32-review-with-correction.md) |
| 33 | Demo "skip to" strip | `/student` (presenter control) | `d3bd7e0` | [architecture/33-demo-skip-to.md](architecture/33-demo-skip-to.md) |
| 34 | Whole-class review: versions beside a pad, frozen or write-with-me | `/student` frozen, `/teacher/whole-class`, `/teacher/board` | `fb9fc69` | [architecture/34-whole-class-follow-modes.md](architecture/34-whole-class-follow-modes.md) |
| 35 | Split view: student, teacher and board in one tab | `/split` (presenter page) | `bff34c5` | [architecture/35-split-view.md](architecture/35-split-view.md) |
| 36 | Class of twenty in five colour groups | `/teacher/groups` | `00d5979` | [architecture/36-class-of-twenty-colour-groups.md](architecture/36-class-of-twenty-colour-groups.md) |
| 37 | Teacher on a laptop: full width, and a viewport guard over every teacher route | `/` teacher card, `/teacher/**` | `02372da` | [architecture/37-teacher-on-a-laptop.md](architecture/37-teacher-on-a-laptop.md) |
| 38 | The smartboard surface: display only, the laptop keeps the controls | `/board`, `/teacher/board` (controls), `/teacher` indicator, `/` card | `d76c226` | [architecture/38-smartboard-surface.md](architecture/38-smartboard-surface.md) |
| 39 | The whole class enters group review together | `/student` class-wait, `/teacher` Class card | `54f397f` | [architecture/39-class-enters-group-review-together.md](architecture/39-class-enters-group-review-together.md) |
| 40 | Group review on one shared whiteboard | `/student` group | `8d9cf9e` | [architecture/40-group-review-shared-whiteboard.md](architecture/40-group-review-shared-whiteboard.md) |
| 41 | The debrief after a correct check | `/student` group, `/teacher/report` | `82ff198` | [architecture/41-debrief-after-a-correct-check.md](architecture/41-debrief-after-a-correct-check.md) |
| 42 | Progress bar, leaderboard and medals | `/board` race and held standings, `/teacher` card, `/student` group bar | `fca135e` | [architecture/42-progress-bar-leaderboard-medals.md](architecture/42-progress-bar-leaderboard-medals.md) |
| 43 | The individual view, and a tidy of the teacher's screens | `/teacher` title line, chips, name links; `/teacher/report?student=`; `/teacher/groups` | `f558209` | [architecture/43-individual-view-and-teacher-tidy.md](architecture/43-individual-view-and-teacher-tidy.md) |
| 44 | Start screen simplified: no skill panel, one row per problem, accent buttons | `/student` overview | `e3466a9` | [architecture/44-start-screen-simplified.md](architecture/44-start-screen-simplified.md) |
| 45 | The teacher pane scales with its height: a 1280 × 800 laptop, fitted like the iPad | `/split` teacher pane | — | [architecture/45-teacher-pane-scales-with-its-height.md](architecture/45-teacher-pane-scales-with-its-height.md) |
| 46 | Class view polish: row buttons, header skills/sub-skills control, New skills, no timestamps, a missing student | `/teacher` grid | — | [architecture/46-class-view-polish.md](architecture/46-class-view-polish.md) |
| 47 | Start screen as a grid of tiles: ten square cards, no chips, WARM UP / START bottom right | `/student` overview | — | [architecture/47-start-screen-tiles.md](architecture/47-start-screen-tiles.md) |
| 48 | Warm-up concerns chat: the picker page gone, one question per ticked skill, then the pad with skill buttons | `/student?stage=warmup-chat`, `…=practice` | — | [architecture/48-warm-up-concerns-chat.md](architecture/48-warm-up-concerns-chat.md) |
| 49 | Group review header: no colour label, names in the group colour, a big progress bar | `/student` group | — | [architecture/49-group-header-tint.md](architecture/49-group-header-tint.md) |
| 50 | Feedback summary: skills as dark purple chips; "What you submitted" / "If needed, correct it here" / "Read as" on one line | `/student?stage=feedback` | — | [architecture/50-feedback-summary-chips.md](architecture/50-feedback-summary-chips.md) |
| 51 | Debrief: a pane that matches the group's rework turns green | `/student` group debrief | — | [architecture/51-debrief-matching-pane-green.md](architecture/51-debrief-matching-pane-green.md) |
| 52 | Group review: Liam's Q7 checks wrong first; a ten-second hold; the ring fits the button and goes once the hold ends | `/student` group | — | [architecture/52-group-wrong-rework-and-hold.md](architecture/52-group-wrong-rework-and-hold.md) |
| 53 | Group review header: the progress bar stays put on "the group got it" | `/student` group whiteboard + debrief | — | [architecture/53-group-header-shared.md](architecture/53-group-header-shared.md) |
| 54 | Whole-class review: the teacher writes on the smartboard, and switches the students' mode from it | `/board` slide, `/student` frozen, `/teacher/board` | — | [architecture/54-board-pad-and-mode-toggle.md](architecture/54-board-pad-and-mode-toggle.md) |
| 55 | Class view: see skills / full breakdown / close, one hover target per student, no board chip | `/teacher` grid, `/teacher/board` heading | — | [architecture/55-class-view-button-words-and-no-board-chip.md](architecture/55-class-view-button-words-and-no-board-chip.md) |
| 56 | Brand header: the real Edexia logo everywhere; the confidence top bar drops "Before you start" | every header, `/student?stage=confidence` | — | [architecture/56-brand-logo-and-confidence-crumb.md](architecture/56-brand-logo-and-confidence-crumb.md) |
| 57 | Student report: the skills laid out as the teacher's class-view row, every group shown at once | `/student?stage=report` | — | [architecture/57-student-report-skill-columns.md](architecture/57-student-report-skill-columns.md) |
| 58 | Student report: a tile per problem in a column per review stage in the pathway; Starred gone; the reflection required before sending | `/student?stage=report` | — | [architecture/58-report-outcome-tiles.md](architecture/58-report-outcome-tiles.md) |
| 59 | Class view: "see dot skills" beside a name opens the row's full breakdown, every group open to its skills | `/teacher` grid | — | [architecture/59-row-see-dot-skills-full-breakdown.md](architecture/59-row-see-dot-skills-full-breakdown.md) |
| 60 | Repo flatten: the app is the repo root, the Sept 7 mockup deleted | — | — | [architecture/60-repo-flatten.md](architecture/60-repo-flatten.md) |
| 61 | Teacher side: the arrow cursor everywhere, never the hand | `/teacher/**` | — | [architecture/61-teacher-arrow-cursor.md](architecture/61-teacher-arrow-cursor.md) |
| 62 | Mistakes view: students side by side, one click opens every student's work in columns | `/teacher/mistakes` | — | [architecture/62-mistakes-side-by-side.md](architecture/62-mistakes-side-by-side.md) |
| 63 | Mistakes view: one pill spans the students who slipped on the same step; the header keeps only the difficulty tag | `/teacher/mistakes` | 62 | [architecture/63-mistakes-shared-pills.md](architecture/63-mistakes-shared-pills.md) |
| 64 | Mistakes view: bigger red-filled slip pills, the question header opens and closes, an expand / close / close-all button on hover | `/teacher/mistakes` | 63 | [architecture/64-mistakes-expand-button.md](architecture/64-mistakes-expand-button.md) |
| 65 | The Edexia bar stays put when the page scrolls, and the board gets one | `/teacher/**` sticky, `/student` verified, `/board` | — | [architecture/65-sticky-brand-header.md](architecture/65-sticky-brand-header.md) |
| 66 | Mistakes view: more room between the student's name and the slip pill | `/teacher/mistakes` | 64 | [architecture/66-mistakes-pill-spacing.md](architecture/66-mistakes-pill-spacing.md) |
| 67 | Mistakes view: the slip pill starts under the avatar, not the name | `/teacher/mistakes` | 66 | [architecture/67-mistakes-pill-avatar.md](architecture/67-mistakes-pill-avatar.md) |
| 68 | Teacher side: the bar never rides the rubber-band; the window stops scrolling and only the content region does | `/teacher/**` | 65 | [architecture/68-teacher-fixed-header-frame.md](architecture/68-teacher-fixed-header-frame.md) |
| 69 | Help chat: a fourth option under "I need help", a tutor that only hints and offers a choice of ways in | `/student?stage=practice`, the practice overlay, `POST /api/help-chat` | — | [architecture/69-help-chat.md](architecture/69-help-chat.md) |
| 70 | Split view: a divider drag lasts exactly as long as the button is held; the window hears the release, a move with the button up ends it | `/split` | 35 | [architecture/70-divider-drag-release.md](architecture/70-divider-drag-release.md) |
| 71 | Start alone, Submit, and the fork: the warm-up is offered on the confidence screen to the student who says they are not confident | `/student` overview, `…?stage=confidence` | 48 | [architecture/71-start-alone-submit-and-the-fork.md](architecture/71-start-alone-submit-and-the-fork.md) |
| 72 | The offer callout: the tutor's question naming the ticked skills, one short problem per skill, a rise and one ring pulse over the dimmed list | `…?stage=confidence` | 71 | [architecture/72-offer-callout.md](architecture/72-offer-callout.md) |
| 73 | Offer callout: "Start the set" in the accent outline, a pinch stronger than a secondary button | `…?stage=confidence` | 72 | [architecture/73-offer-start-the-set-outline.md](architecture/73-offer-start-the-set-outline.md) |
| 74 | Concerns chat rhythm: the opening in two bubbles a second apart, typing dots, a box that is plainly off while the tutor writes and pulses on for the student's turn, a closing bubble before the pad | `…?stage=warmup-chat` | 48, 72 | [architecture/74-chat-rhythm.md](architecture/74-chat-rhythm.md) |
| 75 | Practice pad: "I need help" straight under the question, not at the foot of the column; the fractions warm-up is x/4 + x/2 − 6 = 9/2 with a clearing-denominators hint | `…?stage=practice`, the practice overlay | 69, 29 | [architecture/75-warmup-fractions-help-under-question.md](architecture/75-warmup-fractions-help-under-question.md) |
| 76 | The fraction problem stays wrong after the individual review: Sam's Q7 rework is a second slip, so the group's Q7 is a real struggle | `/student` group, `…?stage=report` | 52 | [architecture/76-q7-rework-still-wrong.md](architecture/76-q7-rework-still-wrong.md) |
| 77 | Hint terms can name a later occurrence of a fragment (`{ tex, within }`); `termTex` works on positioned spans | `…?stage=practice`, the practice overlay | 75 | [architecture/77-hint-fragment-within.md](architecture/77-hint-fragment-within.md) |
| 78 | Several hints per problem, one per ask ("another hint · Show 2 of 2 →", "All shown"), stacked under the problem; the fractions warm-up teaches like terms first: move the 6, then a denominator the two x terms share | `…?stage=practice`, the practice overlay, `POST /api/help-chat` | 77, 69 | [architecture/78-multiple-hints.md](architecture/78-multiple-hints.md) |
| 79 | Every warm-up line is one step and two cases branch side by side: graph features and sketch split into one row per step, the pair checks one fact per row, the worked example card boxes a two-case step like the read-back; a guard test over the bank | `…?stage=practice`, the practice overlay | 75, 29 | [architecture/79-warmup-step-rows.md](architecture/79-warmup-step-rows.md) |
| 80 | Hints that read the student's work: each hint names the point in the working it fits (`at`), "hint" picks by where the lines have got, never for a point passed, "None for this step" when nothing fits; earlier hints collapse to a line | `…?stage=practice`, the practice overlay, `POST /api/help-chat` | 78, 03 | [architecture/80-responsive-hints.md](architecture/80-responsive-hints.md) |
| 81 | Q5's turning point is two lines, the height then the point: the model solution, the scripted read-back and the evaluation table all list five lines | `…?stage=working`, `…?stage=feedback`, `…?stage=history`, the teacher's mirror | 79 | [architecture/81-q5-turning-point-rows.md](architecture/81-q5-turning-point-rows.md) |
| 82 | A hint's linked words point at the student's own line in the read-as column ("your line 4", the line tinted while lit); a lit fraction is boxed whole | `…?stage=practice`, the practice overlay | 80, 77 | [architecture/82-hint-anchors.md](architecture/82-hint-anchors.md) |
| 83 | A lit hint word lights only the line its hint points at, never the same fragment elsewhere; the lit box has a touch more room above and below | `…?stage=practice`, the practice overlay | 82 | [architecture/83-hint-lit-scope.md](architecture/83-hint-lit-scope.md) |
| 84 | The concerns chat names the skill in bold in each ask, and asks the later skills "How about with fractions?" / "How about the null factor law?" | `…?stage=warmup-chat` | 48, 74 | [architecture/84-chat-skill-emphasis.md](architecture/84-chat-skill-emphasis.md) |
| 85 | The factorising warm-up has a hint for every point in the working: the pair, the sum, the brackets, then past the brackets to the null factor law (set each bracket to zero; then x + 3 = 0 and x + 4 = 0 spelt out); the follow-up likewise | `…?stage=practice`, the practice overlay | 80, 82 | [architecture/85-monic-hints.md](architecture/85-monic-hints.md) |
| 86 | "Another hint" opens the chat on the current hint while the student's lines have not moved past what it asks for ("Talk it through →"; the tutor opens with "Let's talk more about hint 2 before another one…", stored in the chat and explained in the brief); the next hint comes once the line is written | `…?stage=practice`, the practice overlay, `POST /api/help-chat` | 85, 80, 69 | [architecture/86-hint-stall-chat.md](architecture/86-hint-stall-chat.md) |
| 87 | The factorising warm-up's second hint no longer lists the pairs to try | `…?stage=practice` | 85 | [architecture/87-monic-hint-trim.md](architecture/87-monic-hint-trim.md) |
| 88 | The factorising hints say "factors" not "brackets"; two abutting lit fragments get a 0.7em gap so their boxes sit clear of each other | `…?stage=practice`, the practice overlay and read-as column | 83, 85 | [architecture/88-factors-boxes.md](architecture/88-factors-boxes.md) |
| 89 | The concerns chat's skill names sit in a light blue box (the standout blue's soft fill and line), not bold | `…?stage=warmup-chat` | 84 | [architecture/89-chat-skill-boxes.md](architecture/89-chat-skill-boxes.md) |
| 91 | The skill box's text is ink, so the chat line reads in one colour | `…?stage=warmup-chat` | 89 | [architecture/91-chat-skill-box-ink.md](architecture/91-chat-skill-box-ink.md) |
| 90 | The worked example is maths alone, every step at the problem's size and centred under it, no captions, no "Guess the next step"; while it plays the right column is the chat headed "Question about a step?" (opener "Which step, and what about it?"), and each turn tells the tutor which steps are on screen | `…?stage=practice`, the practice overlay, `POST /api/help-chat` | 69, 79 | [architecture/90-example-chat.md](architecture/90-example-chat.md) |
| 92 | The help chat sits under the read-as lines instead of replacing them: the read-as list is capped at under half the column while the chat is open (it scrolls), the bubbles gather just above the box to write in | `…?stage=practice`, the practice overlay | 86, 69 | [architecture/92-chat-below-read-as.md](architecture/92-chat-below-read-as.md) |
| 93 | The worked example card is left-justified: the problem, every step, a two-case step's boxes and the reveal button on the card's left edge (`math-left`, an unlayered override of KaTeX's centring) | `…?stage=practice`, the practice overlay | 90 | [architecture/93-example-left.md](architecture/93-example-left.md) |
| 94 | Every worked example step is a ruled row: the same grey rule and the same air (24px each side, 16 compact) between steps as between the problem and the first step; the reveal button keeps the gap, no rule | `…?stage=practice`, the practice overlay | 93 | [architecture/94-example-rows.md](architecture/94-example-rows.md) |
| 95 | "I need help" is gone, not greyed, while the worked example plays; back wherever the pad is live again | `…?stage=practice`, the practice overlay | 90 | [architecture/95-help-hidden-in-example.md](architecture/95-help-hidden-in-example.md) |
| 96 | The lit hint box stops short of the glyph next to it: a thin space between a fragment and a glyph typeset flush against it, at rest as well as lit, and the box padded inside it | `…?stage=practice`, the practice overlay | 88, 83 | [architecture/96-lit-box-clears-neighbour.md](architecture/96-lit-box-clears-neighbour.md) |
| 97 | The lit hint box is no wider than its fragment (padding above and below only) and 7x keeps its typeset spacing: ticket 96's thin space reversed, 7x is one term | `…?stage=practice`, the practice overlay | 96 | [architecture/97-lit-box-narrow.md](architecture/97-lit-box-narrow.md) |
| 98 | The lit hint box fits its surroundings per axis (side air unless a glyph, superscript or the other factor is flush; air above and below unless a numerator or denominator) and the maths keeps its own spacing everywhere (ticket 88's kern reversed; `termTex` never changes the TeX); touching lit boxes parted by a hairline | `…?stage=practice`, the practice overlay | 97, 88, 83 | [architecture/98-factors-no-gap.md](architecture/98-factors-no-gap.md) |
| 99 | "Talk it through" is a pill on the latest hint card (opens the chat on that hint with the pad's stored opener); the help menu is four bare pills (another hint, worked example, video, chat), deep purple borders, the width of the widest, a narrower popup; "another hint" greyed while stalled | `…?stage=practice`, the practice overlay | 86, 95 | [architecture/99-talk-pill.md](architecture/99-talk-pill.md) |
| 100 | A lit box around a whole fraction has 0.2em of air above and below (`hint-term-tall`), where a digit's box keeps 0.08em | `…?stage=practice`, the practice overlay | 98 | [architecture/100-fraction-box-tall.md](architecture/100-fraction-box-tall.md) |
| 101 | The help menu's first pill always reads "hint"; pressed while the previous hint is unacted on it shows a notice ("Let's talk through the previous hint before giving you another." + "Talk it through") that opens the same chat as the hint card's pill | `…?stage=practice`, the practice overlay | 99, 86 | [architecture/101-hint-stall-popup.md](architecture/101-hint-stall-popup.md) |
| 102 | The concerns chat reflects on every answer before the next question ("Gotcha. It sounds like…", "Agreed: that's a tricky skill.", "A lot of students share that struggle.", the last repeating), and closes with the reflection then "Thank you for that insight / those insights. Let's start with ___." | `…?stage=warmup-chat` | 74, 84 | [architecture/102-reflective-listening.md](architecture/102-reflective-listening.md) |
| 103 | The chat under the read-as lines takes only the height it needs, capped at 42% of the column (about the bottom third of the page), and scrolls kept at its end so the latest exchange shows; the read-as list keeps the rest | `…?stage=practice`, the practice overlay | 69, 90 | [architecture/103-chat-bottom-third.md](architecture/103-chat-bottom-third.md) |
| 104 | The conjured 1 hangs in the margin (`\llap`, zero width) so lighting moves nothing anywhere; the lit box pixel sweep is `scripts/hint-box-sweep.mjs` (`npm run sweep:hint-boxes`), part of done for the box | `…?stage=practice`, the practice overlay; `scripts/` | 98, 100 | [architecture/104-conjured-llap-sweep.md](architecture/104-conjured-llap-sweep.md) |
| 105 | "Talk it through" is centred under the hint text in the latest hint card | `…?stage=practice`, the practice overlay | 99 | [architecture/105-talk-pill-centred.md](architecture/105-talk-pill-centred.md) |
| 107 | The concerns chat's closing bubble ("Thank you for those insights. Let's start with ___.") stays up 2.8s before the pad, the length of a whole tutor turn, instead of 1.2s | `…?stage=warmup-chat` | 102, 74 | [architecture/107-close-wait.md](architecture/107-close-wait.md) |
| 108 | The `algebra.expand-factor.nonmonic` leaf's full name is "Non-monic factorisation" (the short form "non-monic factorising" unchanged): the "Which skill?" picker, the confidence and peer screens, the warm-up and help chats | `…?stage=working`, "I need help" | 26 | [architecture/108-nonmonic-rename.md](architecture/108-nonmonic-rename.md) |
| 109 | The practice prompt's card reads "2 minutes on factorising?" (a numeral) with its two sentences in sentence case on two lines: "This is your second mistake on factorising." then "Let's do a short problem to review." | `/student` while working, a second mistake on a group | 31 | [architecture/109-prompt-copy.md](architecture/109-prompt-copy.md) |
| 106 | The fractions worked example writes the 6 as 12/2 ("x/4 + x/2 = 9/2 + 12/2") before combining the numbers: seven steps, the hints after the first placed one line later, the common-denominator hint covering the new line | `…?stage=practice`, the practice overlay | 90, 86 | [architecture/106-six-as-twelve-halves.md](architecture/106-six-as-twelve-halves.md) |
| 110 | The `algebra.expand-factor.monic` leaf's full name is "Monic factorisation" (short form "monic factorising" and the student's "Factorising" override unchanged): the teacher's skills tree | `/teacher/report?student=<id>` | 108 | [architecture/110-monic-rename.md](architecture/110-monic-rename.md) |
| 111 | Once every line of a worded problem's working is read (Q9, Q10), a box at the foot of the pad reads "Provide your final answer as a full sentence."; gone again on undo, never on a "Solve for x" | `…?stage=working`, Q9 after its three lines | 03, 18 | [architecture/111-final-sentence.md](architecture/111-final-sentence.md) |
| 112 | The confidence screen's "not confident with…" list has one "factorising" row that opens "monic" and "non-monic" under it; the row alone means both kinds (`lib/confidence.ts`); the deep-linked survey offers the warm-up | `…?stage=confidence` | 110, 48 | [architecture/112-factorising-subskills.md](architecture/112-factorising-subskills.md) |
| 113 | The confidence screen's spacing is tightened so the list fits the window with factorising's two kinds open (was 80px over) | `…?stage=confidence` | 112 | [architecture/113-confidence-fit.md](architecture/113-confidence-fit.md) |
| 114 | The "full sentence" box is the chat's text field: muted placeholder "Provide your final answer as a full sentence.", the cursor in it as it appears, Enter ends the typing, the sentence kept per problem in `session.answers` | `…?stage=working`, Q9 after its three lines | 111, 74 | [architecture/114-answer-field.md](architecture/114-answer-field.md) |
| 115 | The hand-in check: Hand in over a blank problem opens a card bottom right ("Hand in with Q7 blank?" / "Return to Q7" / "Confirm submit", or "Return to Q2, Q3, Q4" with a blue box under the pointer) instead of handing in; returning puts Hand in in the footer with "Jump to Qn" to its left while another blank remains; a starred problem's tile is the star alone | `/student` while working | — | [architecture/115-hand-in-check.md](architecture/115-hand-in-check.md) |
| 117 | The group board's "we're stuck" mode is removed: no button, no reveal of everyone's earlier work, no scripted press on Jordan's Q3 turn; the action row is Check alone (or "checks when ready") at the right | `…?stage=group` | 40, 76 | [architecture/117-remove-stuck.md](architecture/117-remove-stuck.md) |
| 116 | The handed-in screen stacks two soft boxes: "N problems are incomplete." (a problem is finished once any line, first hand-in or rework, is an answer line, right or wrong; counts down live, gone at zero) above the detective sentence, which names the first submission while anything is outstanding; rows read not attempted / unfinished / N lines; a slip made while finishing a problem blank at hand-in is never counted | `…?stage=feedback`, the post-rework notice | 07, 111 | [architecture/116-incomplete-count.md](architecture/116-incomplete-count.md) |
| 119 | The create screen: a title and the questions typed into tiles in the student's five-wide grid, each tile the editor (typed text on top, the rendered question beneath, live; `x**2`, `1/3`, `sqrt(2)` as KaTeX), a ghost for the next, Enter / Shift+Enter / Backspace / × / paste-split, "Q2 removed. Undo" and Cmd+Z, the draft in the classroom store, Continue pinned to a review stub; the old screen untouched; the bank's Q1 was mirrored to +5x and then restored, the +5x problem living only in the teacher's typed draft | `/teacher/assignments/create`, `…/create/review` | 19, 117 | [architecture/119-create-assignment.md](architecture/119-create-assignment.md) |
| 121 | The create screen opens prefilled: with no draft in the store (first visit, Reset demo) it seeds the title and the demo teacher's ten typed questions from `data/draft-seed.ts` (Q1 `x^2 + 5x + 6 = 0` and a repeated Q9 on purpose, for ticket 120's recommendations); an emptied draft stays empty; from-nothing creation deferred | `/teacher/assignments/create` | 119 | [architecture/121-prefilled-draft.md](architecture/121-prefilled-draft.md) |
| 120 | The review step, replacing the stub: the draft's tiles labelled by difficulty (bank match, fixture, heuristic; a popover to relabel; the pills arriving one by one; the counts), "Assess set" running a five-second bar with three lines, three recommendations matched by expression (change the +5x Q1 to −5x with the class's sign slip as evidence, remove the repeat of Q3, add a problem in a context with three alternatives), each accepted or kept as is with Undo and the grid following, "Finalise set" opening the pathway screen (the unit focus above the map) and Create storing the bank ids, the pathway, the unit and the finalised questions; `PathwayMap` and `UnitFocus` shared with the old screen | `/teacher/assignments/create/review` | 119, 121, 19 | [architecture/120-assignment-review.md](architecture/120-assignment-review.md) |
| 122 | A tap on a difficulty pill on the review step rotates it to the next label (simple familiar → simple unfamiliar → complex familiar → complex unfamiliar → round again) instead of opening the four in a popover | `/teacher/assignments/create/review` | 120 | [architecture/122-label-rotate.md](architecture/122-label-rotate.md) |
| 123 | The review step's unit focus has no Confirm: the inferred unit stands, the note and Reassess remain, Create is on at once; the old screen keeps its Confirm through `UnitFocus`'s optional `onConfirm` | `/teacher/assignments/create/review` | 120 | [architecture/123-no-unit-confirm.md](architecture/123-no-unit-confirm.md) |
| 124 | "Class review" as the stage's title everywhere: the class view card, the setup H1, the pathway map's node and sentence (`STAGE_WORD`), the demo strip's skip button, the status suffix and the home page copy; the chip already said it; stage id, routes and actions unchanged | `/teacher`, `/teacher/whole-class`, `/teacher/assignments/new`, `/` | — | [architecture/124-class-review-title.md](architecture/124-class-review-title.md) |
| 125 | The category-level marker is a pill (28 × 13) on the class grid, the report's browse drill and the student's report row; groups and skills keep their round dots; `StatusDot` gains `shape`, the labels beside a marker clear the pill | `/teacher`, `/teacher/report`, `/student?stage=report` | — | [architecture/125-category-pills.md](architecture/125-category-pills.md) |
| 126 | The category pill's corners follow the header chip: 4 px on the 28 × 13 pill (the chip's 6 px on 22, scaled), the hover area and ring `rounded-md`; dots unchanged | `/teacher`, `/teacher/report`, `/student?stage=report` | 125 | [architecture/126-pill-corners.md](architecture/126-pill-corners.md) |
| 128 | The class grid's row buttons (see dot skills / close, student report) hide while the pointer is over one of the row's category pills: `group-has-[[data-dot]:hover]/row:invisible` on the row-actions div; keyboard focus and the header's controls unchanged | `/teacher` | 125 | [architecture/128-pill-hides-row-actions.md](architecture/128-pill-hides-row-actions.md) |
| 129 | The Pathway card marks where the class is: `lib/classStage.ts` names every stage (indiv working first) as over / current / ahead with the current one's `N/20 done`; over pills navy, the current pill ringed in accent with the count and the group gate line to its right; the Class card gone; the class review card first in the column while a session runs | `/teacher` | 124 | [architecture/129-pathway-stage.md](architecture/129-pathway-stage.md) |
| 127 | A live diagnostic beside every problem on the mistake view: the "Live diagnostic" chip alone until clicked, then the push panel with that problem's own suggested question (one fixture per problem) or one written there; a push belongs to the panel it came from (`pushBelongsTo`), other panels' send waits; the switch reads "respond online" / "not recorded" here and on the class view | `/teacher/mistakes`, `/teacher` | 23, 25 | [architecture/127-mistake-diagnostic.md](architecture/127-mistake-diagnostic.md) |
| 131 | The class grid's row buttons also stay away for two seconds after the pointer last left any category pill (one `pillQuiet` clock for the grid, `PILL_GRACE_MS`), so a sweep across pills never shows them; entering a row without touching a pill shows them at once | `/teacher` | 128 | [architecture/131-pill-grace.md](architecture/131-pill-grace.md) |
| 133 | The drill's group and skill dot chips count as markers like the pills (pointer over one hides the row buttons, leaving one starts the clock), through pointer over/out delegated on each row's tbody against `MARKER`; the grace is one second | `/teacher` | 131 | [architecture/133-dot-grace.md](architecture/133-dot-grace.md) |
| 130 | More mistakes, more kinds: six new wrong lines in the evaluation table (Q1 wrong pair, Q2 sign solving a factor, Q3 expansion sign, Q4 −b, Q7 wrong pair, Q9 −x sign), 45 classmate wrongs instead of 32 in the shape twelve on Q7 (three strategies, six / four / two), Amelia alone on Q6, nobody on Q8; notes and clarifications cover every wrong; the race rows follow the unions | every screen that reads the classmates | 23, 36 | [architecture/130-diverse-mistakes.md](architecture/130-diverse-mistakes.md) |
| 134 | The Pathway card's over pill is the lit skill button's blue (`bg-standout text-white`), not navy ink | `/teacher` | 129 | [architecture/134-over-pill-blue.md](architecture/134-over-pill-blue.md) |
| 132 | The mistake view's diagnostic opens as a flyout from its chip, down and to the right over the blank space, the chip's footprint holding the row; the problem card and every row measure the same open or closed; a flyout that would overrun the window shifts left (`clampToViewport`) | `/teacher/mistakes` | 127 | [architecture/132-diagnostic-flyout.md](architecture/132-diagnostic-flyout.md) |
| 135 | Mistake view: inside each skill pill the students on the exact same wrong line sit together (`mistakeKey`, `groupByMistake`, `SlipGroup.mistakes`) and, open, one box in the pill's red around their working (drawn per cell: edges on the first and last, dividers between; a solo student boxed alone); columns share the card down to a 186 px floor and the working shrinks to fit (`FitGrid` measures and sets `--fit`, 17 px down to 13); names truncate | `/teacher/mistakes` | 130 | [architecture/135-exact-mistake-boxes.md](architecture/135-exact-mistake-boxes.md) |
| 138 | Students whose working is identical line for line share one column on the mistake view: the work written once, every name over it (`WorkColumn`, `groupByWork` under `groupByMistake`; pills, boxes and `start` count columns), so Q7 takes three columns and Q9 three and neither scrolls sideways | `/teacher/mistakes` | 130, 135 | [architecture/138-shared-work-columns.md](architecture/138-shared-work-columns.md) |
| 136 | The roster's student cell: a 16 px name in a fixed 142 px slot (the widest name plus 10) with the **in progress** pill after it on the same line, so every in-progress pill starts at one x; the avatar moves to a new last column after Set so the eye can find its row again; category columns sized to their chips (`columnWidth`: 96, or 132 under "Communication"), chips nowrap, `min-w-[1204px]` fitting the 1280 laptop | `/teacher` grid | 46 | [architecture/136-roster-row.md](architecture/136-roster-row.md) |
| 139 | The diagnostic's "respond online" / "not recorded" switch is gone, and the recorded flag with it: one "send to class" on both views, no pill on the student's modal, `recorded` dropped from the session's diagnostic, its answers and the push action; the finger-raising idea kept in FUTURE_FEATURES | `/teacher`, `/teacher/mistakes`, `/student` | 25, 127 | [architecture/139-no-record-toggle.md](architecture/139-no-record-toggle.md) |
| 137 | The diagnostic's result: the class view's card is a link box until a question is out, then the latest result (question, each option with `n/20 students` and its misconception, the right one green) kept on screen; the run lives on the classroom (`diagnostics`), the classmates' answers trickle in from the push time, the board takes the diagnostic at 20/20 or by the teacher's hand; the flyout shows the same result per tab; `DiagnosticResults` shared; the recorded switch gone | `/teacher`, `/teacher/mistakes`, `/student`, `/board` | 132, 130 | [architecture/137-diagnostic-results.md](architecture/137-diagnostic-results.md) |
| 140 | A small box left of every problem's label on the mistake view counts the class who got it right ("15/20 right": classmates who reached it and are not wrong on it, plus the live student when his hand-in is clean and finished, `rightCount`), its tooltip splitting the rest into the wrong and the unfinished | `/teacher/mistakes` | 138 | [architecture/140-right-count.md](architecture/140-right-count.md) |
| 141 | The roster's avatar at both ends: back before the name as well as closing the row; paid for by sizing each category column to its chip (`columnWidth`: 80 / 88 / 96 / 132 by letters), Confidence 84, the closing column 48 and a tighter pill; column widths and the table's minimum are numbers (`rosterMinWidth`), 1204 in the 1280 laptop's 1208 card | `/teacher` grid | 136 | [architecture/141-avatar-both-ends.md](architecture/141-avatar-both-ends.md) |
| 142 | Mistake view header: the "n/20 right" box sits left of the card level with its header row (`PROBLEM_HEADER`), the difficulty tag after the maths, no live pill on a name, the open flyout 11 px clear of the card (`ml-5`), "send to class" at the panel's bottom right, "complex unfamiliar" light amber; the teacher chrome zoomed 0.72 (was 0.8) on every `/teacher/*` route | `/teacher/mistakes`, `/teacher/*` | 140, 137 | [architecture/142-mistake-header.md](architecture/142-mistake-header.md) |
| 143 | The count box beside each problem reads "n/20 correct" and a second box hangs 6 px under it, "m/20 skipped" (the class less the correct and the wrong: stopped before it, or no answer handed in); one tag style (`COUNT`), the column's top padding centring the first on the header row | `/teacher/mistakes` | 142 | [architecture/143-skipped-tag.md](architecture/143-skipped-tag.md) |
| 144 | The live diagnostic flyout collapses the moment the pointer leaves it (one `onMouseLeave` on the wrapper that holds the chip's footprint and the panel); the tab and a question the teacher was writing are component state, so they are there again on the next click | `/teacher/mistakes` | 132, 137 | [architecture/144-flyout-collapse.md](architecture/144-flyout-collapse.md) |
| 145 | "force submit" beside the Pathway card's current pill for indiv working, indiv review and group review, one at a time, the `N/20 done` count right under it; one advance kind per stage (`force-submit`, `force-review`, `force-group`), `canForce` for enablement, the gate line and the title-line button gone, no confirm step (the minute with Cancel is the undo); `group/end` holds the race where the teacher ended it | `/teacher`, `/student` | 129, 22, 39 | [architecture/145-force-submit-per-stage.md](architecture/145-force-submit-per-stage.md) |
| 146 | The class review setup's Student screens choice has no default: screens frozen and write with me both start empty, "Choose one to project." beneath them and Project off until the teacher picks one; the board's per-problem toggle and the stored-session fallback unchanged | `/teacher/whole-class` | 44 | [architecture/146-choose-screens.md](architecture/146-choose-screens.md) |
| 147 | Pressing the faded Project with no screen mode chosen turns its label to "select one" and flashes both Student screens options light blue for three beats (`.choose-flash`, restarted per press); the button is truly off only with no problem checked | `/teacher/whole-class` | 146 | [architecture/147-select-one-nudge.md](architecture/147-select-one-nudge.md) |
| 149 | The "select one" flash on the Student screens options is one beat (600 ms), not three | `/teacher/whole-class` | 147 | [architecture/149-one-flash.md](architecture/149-one-flash.md) |
| 151 | The Edexia header on every student screen carries the review pathway horizontally, the teacher's Pathway card's idea: a pill per stage of the assignment's pathway, light blue ahead, ringed in purple while current, dark blue with white text once over; pinned beside the student's name; `pathwayStages` in `lib/classStage.ts` decides the stage for both sides | `/student` | 129 | [architecture/151-student-pathway-strip.md](architecture/151-student-pathway-strip.md) |
| 148 | Class review examples chosen by mistake: each slot's header names the exact mistake (every wrong line named in five words in `data/evaluation.ts`) with its count, a flyout menu of the problem's mistakes swaps the working (the largest identical column), the correct working then the most common mistakes suggested, `unit focus` and `fixed in group review` badges (fixed ones sink), the board counts by exact mistake; the problem list untouched | `/teacher/whole-class`, `/board` | 138, 136 | [architecture/148-examples-by-mistake.md](architecture/148-examples-by-mistake.md) |
| 152 | The example picker's menu is as wide as its slot and no wider (`left-5 right-5`), so column C's menu is never clipped by the card; long names wrap, badges inline, the count on the right | `/teacher/whole-class` | 148 | [architecture/152-picker-menu-width.md](architecture/152-picker-menu-width.md) |
| 153 | The confidence screen's Submit sits in the iPad's bottom-right corner on the exact rect of the start screen's START (the same `px-10 … pb-5` frame, the question and answers in a centred `max-w-3xl` column inside it); the empty spot after a not-confident answer is the button itself, invisible | `/student` | — | [architecture/153-submit-bottom-right.md](architecture/153-submit-bottom-right.md) |
| 155 | In the picker's menu a badge (`unit focus`, `fixed in group review`) sits on its own line under the mistake's name, never split; the dot and the count centre on the name-and-badge block | `/teacher/whole-class` | 152 | [architecture/155-badge-own-line.md](architecture/155-badge-own-line.md) |
| 156 | The isolated-practice overlay's footer "Back to Qn →" button is the large size, the same as the finished-state button and "I need help" | `/student` | none | [architecture/156-back-button-large.md](architecture/156-back-button-large.md) |
| 157 | The picker's menu offers only options not already in one of the problem's slots (`taken` from `optionOf` per slot); swapping frees the old option; with none left the header is inert with no chevron | `/teacher/whole-class` | 155 | [architecture/157-picker-unseen-only.md](architecture/157-picker-unseen-only.md) |
| 154 | The teacher writes a goal for the class on the create screen ("Goal for the class", 280 characters, preloaded with Ms Okafor's suggestion); the student reads it once after the overview's CONTINUE (was START, now pulsing an accent ring until pressed), in a speech bubble under "Before you get started / Ms Okafor wants you to knowâ¦", then CONTINUE to the check-in (eyebrow "Check in"); a blank goal skips the screen; `goal` is a `Stage`, `BEFORE_HAND_IN_STAGES` the one list of pre-hand-in stages | `/teacher/assignments/create`, `/student` | 119, 121 | [architecture/154-teacher-goal.md](architecture/154-teacher-goal.md) |
| 150 | Drag to reorder by press-and-hold: the create screen's tiles, the review's difficulty grid and the class review setup's example cards lift after a still 300 ms press and slide the others to make room (`useReorder`, pure `lib/reorder`); a click still edits, a hold never does; the ghost stays last and takes the drop for last; Alt+arrows move a focused item; `draftKey` leaves order out so a reorder keeps the review; the setup's cards stand in assignment order, the list still ranks by struggle, and Project sends the cards' order | `/teacher/assignments/create`, `…/create/review`, `/teacher/whole-class` | 119, 120, 148 | [architecture/150-reorder-questions.md](architecture/150-reorder-questions.md) |
| 158 | In individual review the correction pad reads a line for every problem (`reworkScript`: the scripted correction, else the hand-in's own lines again); Q9's correction is the one line it was missing, so a burst finishes it and the incomplete box counts down | `/student` | none | [architecture/158-review-pad-reads-every-problem.md](architecture/158-review-pad-reads-every-problem.md) |
| 159 | No notice after a rework that leaves no problem wrong: `reworkNotice` in `lib/session.ts` is the final summary's sentence while its count is above zero and `null` otherwise, read by `rework/done` and the teacher's force review; the "N still contain a mistake" notice unchanged | `/student` | 116, 145 | [architecture/159-no-holds-notice.md](architecture/159-no-holds-notice.md) |
| 160 | The press-and-hold that starts a drag fires at 150 ms instead of 300 (`HOLD_MS`), above a click's press-to-release and short enough to read as immediate; one constant, every reordering screen follows | `/teacher/assignments/create`, `…/create/review`, `/teacher/whole-class` | 150 | [architecture/160-shorter-hold.md](architecture/160-shorter-hold.md) |
| 162 | Group review: the board takes the left two thirds and a live "Read as" column the right third (`ReadAs` over the run's shared `lines`, one per burst, on the pen-holder's iPad and every watcher's); the wrong check's cut moves into the column above the live lines, eyebrow "Not yet"; the empty line names the writer; eyebrows level (the column's top padding is the pad's plus the card's border) | `/student` | 40, 52 | [architecture/162-group-read-as.md](architecture/162-group-read-as.md) |
| 163 | The teacher bar's Class · Mistakes · Groups tabs move to the right end beside "New assignment" as indigo pills (the current page deep indigo, white text; the others soft indigo); "New assignment" is a white pill with an ink border and ink text; the brand stands alone at the left | every `/teacher/*` | 65, 142 | [architecture/163-teacher-nav-right.md](architecture/163-teacher-nav-right.md) |
| 164 | No "Group review" crumb beside the wordmark on the group review and class-wait screens: a `null` in the `CRUMB` map is "no crumb", the pathway strip's lit pill is the stage's one name; every other crumb unchanged | `/student` | 151 | [architecture/164-no-stage-crumb.md](architecture/164-no-stage-crumb.md) |
| 161 | In class review the student's screen shows the board's slide: the same examples in the same columns (`ExampleColumns`, shared with the board, lines fitted so none wraps) with a light blue "your initial response" tag on the example that is the student's first hand-in's mistake, no counts; the board's pad is 380 wide and its lines 21 px so nothing wraps at 1440 | `/board`, `/student` | 157 | [architecture/161-student-sees-board.md](architecture/161-student-sees-board.md) |
| 165 | The teacher bar's Class · Mistakes · Groups tabs move back to the left, directly right of the Edexia · Maths brand (`gap-5`, the student header's brand-to-crumb gap); the pills keep ticket 163's colouring; "New assignment", the name and avatar stay at the right end | every `/teacher/*` | 163 | [architecture/165-teacher-nav-left.md](architecture/165-teacher-nav-left.md) |
| 166 | The class-review tag on the student's own example reads "your approach" (was "your initial response") | `/student` | 161 | [architecture/166-your-approach.md](architecture/166-your-approach.md) |
| 167 | The roster's header row (Student, the category chips, Confidence, Set) sticks under the teacher bar while the frame scrolls: every `th` sticky on paper at `z-20` with an inset bottom line, the card `overflow-clip` instead of `overflow-x-auto` so the frame is the heads' scroller | `/teacher` | 165 | [architecture/167-sticky-roster-head.md](architecture/167-sticky-roster-head.md) |
| 168 | The assignment title is the crumb beside the wordmark on every student screen (`crumb = title` in `StudentApp`; the per-stage `CRUMB` map, ticket 164's empty crumb included, is gone): the strip names the stage, the screen names itself, the header never changes | `/student` | 151, 164 | [architecture/168-title-crumb-everywhere.md](architecture/168-title-crumb-everywhere.md) |
| 169 | The teacher's student report opens to the class view's full dot view, fixed: `SkillColumns` in `expanded` mode, `locked` (group rows fixed `div`s, skills still open their work, the commentary filter still lights); the browse drill (`HierarchyDrill`'s default export) removed | `/teacher/report` | 125 | [architecture/169-report-dot-view.md](architecture/169-report-dot-view.md) |
| 174 | Both reports' category header band is gone: each category pill carries its name in white on the status colour, every pill in the row the widest name's width (the names stacked invisibly in each), the text size fitted to the row (11 px teacher, 9 px student); UNIT 1 stays grey to the right of NEW SKILLS; the student's reflection panel 320 px (was 440) | `/teacher/report`, `/student` (report) | 169 | [architecture/174-labelled-pills.md](architecture/174-labelled-pills.md) |
| 170 | Problem extraction: `POST /api/extract` takes typed text, images and PDFs (native document blocks) and streams back one draft per problem as JSON lines in reading order (stem, TeX, label, page, figure box; no solutions), a `done` per source; caps 10 MB, 20 images, 5 PDFs, 10 pages; `EXTRACT_FIXTURES=1` answers from fixtures rendered from the demo set, matched by hash | `POST /api/extract` | 69 | [architecture/170-extraction-route.md](architecture/170-extraction-route.md) |
| 175 | Category history on the class view: a third row button **see history in…** fades everything but the student, widens their pills to their category names in white, and a pill click stacks the last five dated results above it (`lib/history.ts`, seeded mixes around today's status; Priya dark green throughout) over a cream-deep sheet from Algebra to Set whose top cuts a pill halfway (the top rows: the heads covered, the cream over the "due" line), drawn beside the clipped card by `HistoryBlocker` (ResizeObserver, z-25) with the stacks at z-30; a drill and history coexist; **close history** or another row's click leaves | `/teacher` | 125, 141, 167 | [architecture/175-category-history.md](architecture/175-category-history.md) |
| 176 | "New assignment" leaves the teacher bar (the right group is the name and avatar) and heads the class view's right column, right above the Pathway card, flush with its left edge and level with the roster card's top: a light indigo pill (`bg-accent-soft`) with deep indigo text and 1 px border (`accent-deep`), the accent line tint on hover | every `/teacher/*`, `/teacher` | 163, 165 | [architecture/176-new-assignment-above-pathway.md](architecture/176-new-assignment-above-pathway.md) |
| 177 | The third row button reads **see history** (was "see history in…"); the three row buttons a pinch bigger and further apart (11.5 px text, 2 px above and below, 3 px between: 15.5 each, 52.5 the stack), every roster row 6.5 px taller (81.5); the cream's cut keeps at least 6 px above the stacks (`HISTORY_CLEAR_PX`), since the taller rows put the row above's pill midline exactly on the stacks' top | `/teacher` | 175 | [architecture/177-row-buttons-pinch.md](architecture/177-row-buttons-pinch.md) |
| 178 | No pathway strip on the student's report or the two screens it opens (peers, own working): `stages` is `[]` at those stages in `StudentApp`, so the header's right end is the name and avatar alone and the strip's space stays blank; every earlier screen keeps ticket 151's strip | `/student` | 151 | [architecture/178-no-strip-on-report.md](architecture/178-no-strip-on-report.md) |
| 180 | The class view's row buttons come back at once left of the row's first pill: a delegated `onPointerMove` on each student's tbody (`markerMove`) ends ticket 131's one-second grace the moment the pointer's x is left of the row's first `[data-dot]` button, off every marker; to the right, between and beyond the pills, the second still holds, and a faded row in history mode still shows nothing | `/teacher` | 131, 175 | [architecture/180-quiet-left-of-pill.md](architecture/180-quiet-left-of-pill.md) |
| 181 | History mode's five dated pills spread evenly from the sheet's top to today's pill; the sheet white (paper) over the category columns only (Algebra through New skills, no apron); today's named pill is the roster's own `StatusDot` with a `label` (the same element and 13 px height as a plain pill) and the five are the same `StatusDot`s at its width, drawn by `HistoryBlocker` from measured pill rects | `/teacher` | 175, 177 | [architecture/181-history-spread-white.md](architecture/181-history-spread-white.md) |
| 171 | The create screen takes pictures: the grid is the drop target (an overlay, nothing moves), ⌘V pastes a screenshot, the ghost reads "Type a question, or drop a picture" with an Upload link; each file gets a shimmer tile while `/api/extract` reads it and its drafts stream in before it as tinted unconfirmed tiles (✓ keep, × discard, thumbnail, the sheet's number); the bar gains "Discard N · Add N" with Continue live and confirming all; twenty images per drop, ten MB each, the rest named in a note; failures are message tiles; files in IndexedDB; the tile's text for an uploaded question is stem then TeX, `$…$` explicit inline, TeX passed through | `/teacher/assignments/create` | 170, 119 | [architecture/171-drop-paste-upload.md](architecture/171-drop-paste-upload.md) |
| 179 | The class view's "New assignment" pill leaves the right column for a row of its own between the due line and the cards, over the right column's left edge (a second `grid-cols-[1fr_320px]` row, the cards' grid `mt-6` beneath); the roster card and the Pathway card start level again, the Pathway card's top on the category heads' top as before 176 | `/teacher` | 176 | [architecture/179-new-assignment-row.md](architecture/179-new-assignment-row.md) |
| 172 | The create screen takes PDFs: five a drop, ten pages each (counted in the browser with `pdfjs-dist` before sending, refused on the tile past the cap), sent to `/api/extract` as native documents; each draft's tile carries its page drawn by pdfjs as its thumbnail and "N · p. M" beside its label; several files run in parallel into their own groups; a docx is named "export it as a PDF"; the ghost reads "…or drop a picture or PDF" | `/teacher/assignments/create` | 171 | [architecture/172-pdf-upload.md](architecture/172-pdf-upload.md) |
| 173 | Typed questions go through the model as the focus leaves a changed tile (the shorthand parser's preview until the reading lands, then the model's stem and TeX, no tint; not configured, the parser's stands quietly); a "Fix" line under every focused tile's text sends a plain-language correction or a line of TeX with the source picture and replaces the tile's text with the answer (`mode: "fix"` on the route, `fixtureFix` in fixture mode); a figure the model boxed is cut from the image or the PDF page at 2× (`lib/crops`), kept in IndexedDB, shown under the question and on the review grid | `/teacher/assignments/create`, `…/create/review`, `POST /api/extract` | 171, 172 | [architecture/173-typed-through-model-fix-figures.md](architecture/173-typed-through-model-fix-figures.md) |
| 182 | Both student "I need help" buttons (the working screen's, the practice pad's) carry a 1 px deep indigo border: `Button`'s new `deep` variant (paper, ink text, `accent-deep` border, `accent-soft` fill on hover) replaces `secondary` on them | `/student` | none | [architecture/182-help-border.md](architecture/182-help-border.md) |
| 183 | Every Continue, Submit and Send in a bottom-right corner (overview and goal CONTINUE, check-in Submit, diagnostic and report Send, warm-up and help chat send, the create screen's Continue) fires from a press up to 12 px outside its pill: `Button`'s `hit` prop draws an absolute `::before` 12 px beyond the pill on every side (8 px on the textarea side of a chat send), part of the button for hit-testing, nothing moved; the create screen's focused ghost no longer clips its placeholder | `/student`, `/teacher/assignments/create` | none | [architecture/183-cta-hit-area.md](architecture/183-cta-hit-area.md) |
| 184 | The class is "11 Methods" (code 11MAM2) on the QCAA Mathematical Methods 2025 syllabus (Unit 1 · Topic 1 · Surds and quadratic functions), today's set is "Problem Set 2 — Roots of a quadratic", and every header reads "Edexia · 11 Methods": the fixture's names, `Assignment.classCode`, `Brand`'s default suffix, the tab title; plus the Edexia Classroom run's tickets 184–189 and its future features | all | none | [architecture/184-class-naming.md](architecture/184-class-naming.md) |
| 185 | Assignments have ids and their own pages: `lib/assignments.ts` (the registry: `assignmentBundle(id, classroom)`, `landingTab`, `rosterProgress`, `assignmentHref`) read through `AssignmentProvider` from `app/teacher/a/[id]/layout.tsx`; Class (`/class`), Mistakes and Groups tabs per set, `/teacher/a/<id>` lands on Class once everyone is in or past working, else Mistakes; "← Edexia Classroom" above every assignment page's eyebrow; force submit on Mistakes too; the pill beside a student's name reads "Q4 in progress" / "warming up" (`lib/progress.ts`) until they hand in, their pills not-seen till then; each set's own frozen groups (`ClassroomState.assignmentGroups`, `migrateClassroom`) apart from the class defaults at `/teacher/groups`; `/teacher` a plain list with New assignment; `/teacher/mistakes` redirects; `/teacher/assignments/new` deleted; side fix: the student header no longer wraps with a four-stage pathway | `/teacher`, `/teacher/a/<id>/…`, `/teacher/groups` | none | [architecture/185-assignments-and-routes.md](architecture/185-assignments-and-routes.md) |
| 186 | Edexia Classroom at `/teacher`: eyebrow "11MAM2 · Mathematical Methods · 20 students", "Edexia Classroom" with "+ New assignment" on the title row, the assignments as whole-card links on the chrome's full width, each titled by the registry's sentence-case `name` in the display serif, LIVE (still in individual working: "● live · n/20 submitted · n mistakes so far", the dot breathing) above PAST ("in review" or "done" · n/20 submitted · top gap), newest first; `lib/classroomCards.ts` derives every card from the bundle, the classroom, Sam's session and `now` (`mistakeCount` = the Mistakes tab's rows, `topGap` = the biggest exact slip cluster across problems); the class Groups page gains "← Edexia Classroom" | `/teacher`, `/teacher/groups` | none | [architecture/186-edexia-classroom.md](architecture/186-edexia-classroom.md) |
| 187 | Problem Set 1 — Features of a parabola, finished: `data/pset1/` (ten problems on the three forms, ids `ps1-q1`…, its evaluation table read by `evaluateLine` beside Problem Set 2's, all twenty students' records with Sam's `PS1_SAM`, Liam missing), registered in `lib/assignments.ts` as `kind: "finished"` with `name`, pathway, classmates, `sam` and frozen default groups; Class View rows, Mistakes rows and the report read Sam's record on a finished set (`AssignmentBundle.sam`, `studentRecord`); every set's report at `/teacher/a/<id>/report` (`/teacher/report` redirects); a finished set hides the live-lesson cards and the live diagnostic; history: `lib/setHistory.ts` puts each student's real Problem Set 1 category status as Problem Set 2's newest pill (Sep 3) after four simulated August pills, the same four on Problem Set 1's own history; the Classroom's PAST card reads done · 19/20 · top gap non-monic factorising; Mistakes' count column fixed width | [architecture/187-problem-set-1.md](architecture/187-problem-set-1.md) |
| 188 | Create starts blank: "New assignment" opens a greyed, `inert` title, goal box and ghost Q1 (`BlankStart`) with a pulsing "Generate simulated assignment" centred on the grid; Generate stores the seeded draft flagged `generated` (`lib/draft.ts`), the tiles fade in with nothing moving, Continue appears; the pathway step's "Confirm groups" (`SeatingBoard`, lifted out of the Groups page) pre-fills the class defaults and keeps moves on the review; Create registers Problem Set 2 with `startedAt` and its frozen groups and lands on its Mistakes; before Create Problem Set 2 is absent (`PROBLEM_SET_2_BEFORE_CREATE = false`) and its pages say so with a New assignment link; skips create it an hour in the past; Reset returns to blank | `/teacher/assignments/create`, `/teacher/assignments/create/review`, `/teacher/a/pset-2/…` | none | [architecture/188-create-from-blank.md](architecture/188-create-from-blank.md) |
| 191 | A finished set's Class View drops the Pathway card (inside `TeacherLive`'s existing `finished` guard), so its side column starts with the Key level with the table; the key's half dot reads "incomplete" (`StatusKey`, also on the student report) | `/teacher/a/pset-1/class`, `/teacher/a/<id>/report` | none | [architecture/191-finished-class-view-trim.md](architecture/191-finished-class-view-trim.md) |
| 190 | Every confidence label legible on Class View: `confidenceForms` in `lib/report.ts` lists a label's forms (every named skill, each alone with "+n", "low +n"); `ConfidenceCell` measures them unseen inside the cell and shows the first that fits the 84 px column in three 17 px lines at 13 px (no `FitText` shrink), the whole answer as `title` and screen-reader text; the row and column unchanged | `/teacher/a/<id>/class` | none | [architecture/190-confidence-label.md](architecture/190-confidence-label.md) |
| 192 | Class View's Key card rides the bottom of the view as the roster scrolls (`sticky bottom-12` in a `flex-1` box at the end of `TeacherLive`'s side column), on finished and live sets; the cards above it stay at the column's top | `/teacher/a/<id>/class` | none | [architecture/192-sticky-key.md](architecture/192-sticky-key.md) |
| 193 | The create screen's pathway step labels (Unit focus, Review pathway, Confirm groups) are the accent chip Class View uses for Pathway and Live diagnostic (`DIAGNOSTIC_CHIP` on `UnitFocus` and `PathwayStep`) | `/teacher/assignments/create/review` | none | [architecture/193-pathway-step-label-chips.md](architecture/193-pathway-step-label-chips.md) |
| 195 | The Mistakes tab's stage group (stage pill, done count, force submit) ends on the problem cards' right edge: `TeacherMistakes`' title row mirrors a problem row, a `flex-1` box with the title and group `justify-between`, then the Live diagnostic chip's unseen footprint (`DiagnosticFootprint`, exported from `DiagnosticPush`) holding the diagnostic column's width | `/teacher/a/<id>/mistakes` | none | [architecture/195-mistakes-stage-right.md](architecture/195-mistakes-stage-right.md) |
| 197 | The create screen's review pathway map draws its arrows from the stage picked in each column to every option in the next (`mapColumns` in `lib/pathway.ts` gives each column's `from` row; `PathwayMap`'s fixed-height nodes let an `Arrows` SVG curve between row middles without measuring), the picked arrow ink and its siblings faded | `/teacher/assignments/create/review` | none | [architecture/197-pathway-arrows-follow-pick.md](architecture/197-pathway-arrows-follow-pick.md) |
| 196 | Class View's cards above the Key (class review, Pathway, group progress, Live diagnostic) rise with the page and stay 48 layout px under the bar (`sticky top-12` group in `TeacherLive`'s side column); `useSideColumnPins` lets the top group scroll when the window can't fit it and the Key both | `/teacher/a/<id>/class` | none | [architecture/196-sticky-side-cards.md](architecture/196-sticky-side-cards.md) |
| 199 | The Mistakes tab's force submit a size larger: `ForceSubmit`'s `inline` button 13.5 px with `px-3 py-1`, just under the stage pill; Class View's copy unchanged | `/teacher/a/<id>/mistakes` | none | [architecture/199-force-submit-larger.md](architecture/199-force-submit-larger.md) |
| 201 | Once a column of the create screen's review pathway map has a pick, only the arrow to the pick is drawn (`Arrows` in `PathwayMap` skips the unpicked options); the unpicked boxes stay faded and tappable | `/teacher/assignments/create/review` | none | [architecture/201-pathway-arrows-picked-only.md](architecture/201-pathway-arrows-picked-only.md) |
| 200 | "← Edexia Classroom" is a dark purple button: `BackToClassroom` (`AssignmentContext.tsx`) white text on the new `accent-dark` token (#2f2491, `globals.css`), rounded, hover `accent-deep`, the arrow kept; every assignment and Create page shares it | `/teacher/a/<id>/*`, `/teacher/groups`, `/teacher/assignments/create`, `/teacher/assignments/create/review` | none | [architecture/200-back-to-classroom-button.md](architecture/200-back-to-classroom-button.md) |
| 202 | The smartboard's class review examples drop the "n/m students" corner: `boardExamples` (`lib/examples.ts`) carries only letters and lines, so the board has no count to show; the teacher's setup keeps its per-example counts from the picker options | `/board`, `/teacher/whole-class` | none | [architecture/202-board-no-counts.md](architecture/202-board-no-counts.md) |
| 189 | Problem Set 2's class streams in from Create, one problem submission at a time: `data/stream.ts` (each classmate's pace, the five who warm up first, Jordan stalling on Q8, Chloe never starting) read by the pure `lib/stream.ts` (`scheduleFor`, `stateAt`, `recordAt`, `classmatesAt` from `AssignmentBundle.startedAt` and `now`); `rosterProgress`, `classStages`, `mistakesByProblem` (rows by arrival, `pending`) and the Classroom card read it; `lib/arrivals.ts` holds the Mistakes cards at or above the pointer and glows each arriving name (`.arrive`); Jordan's record to Q7, three confidences to "confident" | `/teacher`, `/teacher/a/pset-2`, `/teacher/a/pset-2/class`, `/teacher/a/pset-2/mistakes` | none | [architecture/189-live-stream.md](architecture/189-live-stream.md) |
| 194 | The mistake view's Live diagnostic flyout reads at the problem cards' size (question 17 px, options 16 px, 460 px panel, `size="panel"` on `DiagnosticResults`); app-wide, `.katex` is `white-space: nowrap` so typeset maths never splits across lines | `/teacher/a/<id>/mistakes`, every route with maths | none | [architecture/194-diagnostic-text-maths-nowrap.md](architecture/194-diagnostic-text-maths-nowrap.md) |
| 198 | The chat opens on a hint two ways: the stall notice's "Talk it through" says "Let's talk about hint n before another one. What is the hint asking you to do, in your own words?" (`hintOpener`), the hint card's pill says only the question (`TALK_OPENER`); `PracticePad.talkHint(text)`; the chat sends the hint cards (`hinted`) so the brief lists them by number | `/student` practice pad | none | [architecture/198-hint-chat-openers.md](architecture/198-hint-chat-openers.md) |
| 203 | Every warm-up has a hint for every point in its working (`at: [k]`, linked words on the student's line), so "hint" gives a card or leads into the stall notice and the chat until the last line; tests hold every warm-up to it; the hint-box sweep opens all 15 warm-ups (`scripts/warmup-leaves.json`), reads every line and passes the stall notice | `/student` practice pad | 198 | [architecture/203-warmup-hints-every-line.md](architecture/203-warmup-hints-every-line.md) |
| 204 | Every "Talk it through" (hint card, stall notice) and the menu's "chat" put the cursor in the chat's box, even with the chat already open: `PracticePad` counts asks (`chatAsks`, `openChat`) and `HelpChat` refocuses on each (`asked`); the worked example's chat still waits | `/student` practice pad | 198 | [architecture/204-talk-jumps-to-chat.md](architecture/204-talk-jumps-to-chat.md) |
| 205 | A warm-up skill the student leaves, by "Next skill" or a chip tap, finished or not, counts as done (`leaveSkill` in `lib/session.ts`), so its chip turns dark and "Next skill" never loops back to it; once every skill has been opened the button is "On to the set" | `/student` warm-up | none | [architecture/205-warmup-skill-visited.md](architecture/205-warmup-skill-visited.md) |
| 206 | The fractions warm-up has a hint per line: after "… = 9/2 + 6" write the 6 over 2, after "… = 9/2 + 12/2" add the two numbers (linked to the 2s and the numerators on the student's line), after "… = 21/2" the x terms' common denominator (was one hint for all three lines) | `/student` practice pad | 203 | [architecture/206-fractions-number-hints.md](architecture/206-fractions-number-hints.md) |
| 207 | The mistake view's Live diagnostic example options are an even two-column grid (`grid-cols-2` in `DiagnosticPush`, each option's maths in a 16 px `FitText`), matching the result grid after a send | `/teacher/a/<id>/mistakes` | none | [architecture/207-diagnostic-option-grid.md](architecture/207-diagnostic-option-grid.md) |
| 218 | The group debrief has nothing to write: the three versions stay unmarked for 5 s from the group's check (`UNMARKED_MS`, `marksOpen`), then the marks open on their own and Next waits the 10 s hold beside "take a moment to reflect"; the prompt, the note actions and the teacher report's group notes are removed | `/student` group review | none | [architecture/218-debrief-marks-open-on-their-own.md](architecture/218-debrief-marks-open-on-their-own.md) |
| 219 | The group debrief's unmarked view holds 2 s before the marks open (`UNMARKED_MS`), and a peer's next turn waits 13 s (`PEER_DEBRIEF_MS`: 2 s + the 10 s hold + a press) | `/student` group review | 218 | [architecture/219-debrief-two-seconds.md](architecture/219-debrief-two-seconds.md) |
| 220 | A read before group review: "Group review", two paragraphs on working as a team and the group's problems as tiles, nothing about who got what wrong; no button, the board opens on its own 39 s (`GROUP_INTRO_MS`, 75 words at 130 wpm + 4 s) after the class went in (`readiness.startedAt`), via the run's `startedAt`, so peers' turns and the race wait too; shown on the teacher's forced end of individual review, the forced notice held until the board | `/student` group review | none | [architecture/220-group-intro.md](architecture/220-group-intro.md) |
| 208 | Problem Set 2 is Problem Set 6 (`pset-6`, Thu 10 Sep) and Problem Set 1 is Problem Set 5 (`pset-5`, now Mon 7 Sep; `data/pset5/`, problem ids `ps5-q*`) everywhere a set is named; `lib/renamedSets.ts` maps the old ids and seeded titles, read by `next.config.ts` redirects (`/teacher/a/pset-2/…` → `pset-6`) and by `migrateClassroom` so stored demo state loads under the new ids; `LIVE_SET_BEFORE_CREATE` | `/teacher`, `/teacher/a/pset-6/…`, `/teacher/a/pset-5/…`, `/teacher/assignments/create`, `/student` | 187, 188 | [architecture/208-rename-sets-6-and-5.md](architecture/208-rename-sets-6-and-5.md) |
| 216 | The Edexia Classroom pins its heading and Live section: eyebrow, title, + New assignment, LIVE and the live card are one `sticky` cream region (`data-classroom-pinned`, out over the chrome's padding so every rect at scroll 0 is unchanged); only Past scrolls, under its edge, in the chrome's one scroll region; a focused card clears the edge (`--classroom-pinned` scroll margin); nothing live means no Live section, Past in its place; each section sorts newest due first (`dueOrder`, `newestFirst` in `lib/classroomCards.ts`) | `/teacher` | 208 | [architecture/216-classroom-pinned-live.md](architecture/216-classroom-pinned-live.md) |
| 221 | From a problem's second wrong check the group whiteboard shows a hint under "Not yet": the evaluation table's clue for the latest attempt's first wrong line (`boardHint`, `HINT_AFTER_WRONG`), in the practice pad's `HintCard`; Liam's Q7 turn goes wrong twice before the model solution | `/student` group review | none | [architecture/221-group-board-hint.md](architecture/221-group-board-hint.md) |
| 209 | New skills is per set: every skill has one home (null factor law in Functions › Zeros, the discriminant in Algebra › Equations, the binomial identity, merged with special products, in Algebra › Expanding & factorising; `LEAF_ALIASES` resolves old ids), each set lists its `newSkills` (Set 6: discriminant, null factor law; Set 5: null factor law, binomial identity) and `hierarchyFor(ev, set)` rolls a listed skill up under New skills instead of its home, never both; Create's pathway step infers them (`inferNewSkills`: a focus skill not assessed under its home in the last two sets) as switchable chips, Create always on; the "Unit 1" label and `lib/unit.ts` are gone; class review's badge reads "new skill" | `/teacher/a/<id>/class`, `/teacher/a/<id>/report`, `/teacher/assignments/create/review`, `/teacher/whole-class`, `/student` report | 208 | [architecture/209-new-skills-per-set.md](architecture/209-new-skills-per-set.md) |
| 222 | A third wrong check on the group whiteboard leaves the problem for now (6 s pause, `group/leave`); the board returns to it after the union with the next pen (`visitsOf`, derived from `left` and the seed), and a wrong check there closes it unsolved; closed problems count on the bar, the unsolved debrief shows the group's last try; the demo group never solves Q7 (four wrong tries), and a scripted peer clears the board before each later try | `/student` group review, `/teacher/a/<id>/class` | 221 | [architecture/222-group-leave-return-unsolved.md](architecture/222-group-leave-return-unsolved.md) |
| 223 | A problem the group could not get shows for the teacher and the student: the class view's group card says "Q7 left for now after 3 tries", then "Q7 not solved after 4 tries" (`stuckProblems`, `GroupStanding.stuck`); class review setup badges the group's mistakes on it "not solved in group review" (`unsolvedInGroup`); the report notes it under Incorrect | `/teacher/a/<id>/class`, `/teacher/whole-class`, `/student` report | 222 | [architecture/223-unsolved-in-group-teacher.md](architecture/223-unsolved-in-group-teacher.md) |
| 224 | The group intro holds a fixed 30 s (`GROUP_INTRO_MS`), not the 39 s ticket 220 derived from the words; the words-based timing is removed | `/student` group review | 220 | [architecture/224-group-intro-thirty.md](architecture/224-group-intro-thirty.md) |
| 225 | The student's own report shows the teacher's dot key (`StatusKey`, percentages included) under the skills in its Skills card | `/student` report | 169, 209 | [architecture/225-student-report-key.md](architecture/225-student-report-key.md) |
| 226 | Every way into group review shows the description first: links naming `class-wait` / `group` restart group review (`group/restart`: no run, not arrived) instead of resuming a stored run, and the gate and board effects wait for the clock's first tick, so a run never begins at 0 with its board already open; the skip bar and the played-through pathway verified from every stage | `/student` group review | 220, 224 | [architecture/226-group-intro-every-entry.md](architecture/226-group-intro-every-entry.md) |
| 215 | Class View history pills read the Classroom's real earlier sets: a category's stack is the last five earlier sets that assessed it (New skills: every earlier set with New skills), oldest first; real pills read "PS5 · Mon 7 Sep" and link to that set's Class View opened on the student's history (`?history=&open=`), simulated pills fill the top dated a week before the first set and on weekdays up to it, walked one colour step at a time (`simulatedWalk`, the shuffle gone); in history mode a pill fills its column so the labels fit; every set × student × category checked for jumps (20 real Set 5 → Set 6 jumps listed for ticket 210) | `/teacher/a/<id>/class` | 209 | [architecture/215-history-real-sets-linked.md](architecture/215-history-real-sets-linked.md) |
| 227 | The student's report shows the full dot view, fixed, with the key above Reflection; the teacher's student report is at 125% (`TeacherChrome` `zoom`); `fitLabels` counts node padding and caps wrapping at two lines | `/student` report, `/teacher/a/<id>/report` | 225, 169 | [architecture/227-report-full-dots.md](architecture/227-report-full-dots.md) |
| 228 | Group review's debrief goes by itself when its hold ends (`debriefEndsAt`, the clock loop) and only the latest closed problem debriefs, so a problem the group is past never comes back (reproduced: an unfinished Q1 debrief returned after Q2's and Q3's); the demo's fixed pens (`DEMO_PENS`, `GroupRun.pens`) have Sam write Q1 and both visits to Q7, an exception to the shuffle for the simulation only | `/student` group review | 222 | [architecture/228-group-review-flow.md](architecture/228-group-review-flow.md) |
| 229 | The practice pad's "I'd like a…" help menu has a small × in its top-right corner (`data-help-close`), calling the menu's `onClose` like a scrim click or Escape | `/student` practice | none | [architecture/229-help-menu-close.md](architecture/229-help-menu-close.md) |
| 210 | Finished sets are one list: a set is a folder `data/psetN/` exporting a `FinishedSet` (`data/finishedSet.ts`) and one line in `data/finishedSets.ts` (blank-separated slots for PS1–PS4); `lib/finishedSets.ts` orders them by due date and the registry, the evaluation index and frozen groups are built from it (Set 5 moved on with no behaviour change). A shared suite (`data/finishedSets.test.ts`) checks every set, including equality with the class story sheet: `data/story.ts` (20 students × 6 categories × Sets 1–6, habits and problems, outlines for Sets 1–4), rendered to `specs/class-story.md` (`npm run story:sheet`), tested complete, one step at a time and equal to Set 6's end state. Set 5's work changed for nine students so nothing jumps to Set 6; its top gap is now graph features. | `/teacher`, `/teacher/a/pset-5/*`, `/teacher/a/pset-6/class` (history) | 209, 215 | [architecture/210-earlier-sets-groundwork.md](architecture/210-earlier-sets-groundwork.md) |
| 231 | The practice pad's stall notice ("Let's talk through the previous hint…") has the same corner ×; both cards now draw one `CardClose` (`data-card-close`, replacing 229's `data-help-close`) | `/student` practice | 229 | [architecture/231-stall-notice-close.md](architecture/231-stall-notice-close.md) |
| 230 | The group debrief's own panes read "Your first submission" and "Your second submission" (were "Handed in" and "Reworked"); "Group's rework" / "Group's last try" unchanged (`markedVersions` in `lib/debrief.ts`) | `/student` group review | 218, 222 | [architecture/230-debrief-submission-labels.md](architecture/230-debrief-submission-labels.md) |
| 232 | The group intro's bar and time left move in step: both drawn every frame (`useFrameNow`) from the one run start, the label rounded to the nearest second (`introSecondsLeft`), so "0:15" shows a bar at half; the CSS drain animation removed | `/student` group review | 224 | [architecture/232-intro-bar-in-step.md](architecture/232-intro-bar-in-step.md) |
| 234 | Problem Set 6 stays in Live until class review ends: a live set's section is `live` while any stage is current and `past` once class review has ended, tagged `in review` through every review stage; a card's line follows its status; the chips overhang the line so every card is one height and the pinned Live card never grows | `/teacher` Classroom | 186, 216 | [architecture/234-live-until-review-ends.md](architecture/234-live-until-review-ends.md) |
| 233 | The student's report fits one screen (no set eyebrow, mastery in the title row, What happened sized by tiles); a Q tile or skill opens its marked working in the side column in place of key + reflection, a press elsewhere closes it, Send closes it and nudges for a reflection (`lib/reportWork.ts`, `ProblemWork`) | `/student` report | 227 | [architecture/233-report-work-column.md](architecture/233-report-work-column.md) |
| 235 | Group review's Not yet card shows every line of a wrong attempt, its first mistake red (`markFirstMistake`, no "more lines" count), and a wrong check wipes the board's strokes as well as its lines, so the next attempt starts clean | `/student` group review | 221, 222 | [architecture/235-not-yet-whole-board-clears.md](architecture/235-not-yet-whole-board-clears.md) |
| 211 | Problem Set 1 — Surds as a finished set: `data/pset1/` (ten surd problems, every written line in the evaluation table, twenty full records) and its line in `data/finishedSets.ts`; card 20/20, top gap surds (7); the sheet's PS1 column gains three habits so every problem has a wrong student; `pset-1` is no longer mapped to `pset-5` (`lib/renamedSets.ts`); the registry-wide history jump test skips a real pair with an unregistered sheet set between | `/teacher`, `/teacher/a/pset-1/*`, `/teacher/a/pset-5/class` and `/teacher/a/pset-6/class` (history) | 210, 208, 215 | [architecture/211-problem-set-1-surds.md](architecture/211-problem-set-1-surds.md) |
| 212 | Problem Set 2 — Rationalising and expanding with surds is a finished set (`data/pset2/`): ten hand-checked surd problems, all twenty students' work matching the class story sheet's Set 2 column, top gap binomial identity (9); `pset-2` is no longer an old id (no redirect to Set 6, its stored seating its own) | `/teacher`, `/teacher/a/pset-2/*`, `/teacher/a/pset-6/class` (history) | 210, 215 | [architecture/212-problem-set-2-rationalising.md](architecture/212-problem-set-2-rationalising.md) |
| 236 | The student header's set title reads "PSET 6 — ROOTS OF A QUADRATIC" (`crumbTitle` in `lib/crumbTitle.ts` shortens a leading "Problem Set N"), so it fits beside the pathway strip untruncated; the stored title and every other screen keep "PROBLEM SET 6" | `/student` header | 168, 185 | [architecture/236-pset-crumb.md](architecture/236-pset-crumb.md) |
| 238 | Group review's wrong check (first or second on a first visit) pops a "Try again" pill mid-screen on every member's iPad: light blue, dark purple border and text, one pulse, fades in 1.6 s; after the second, the hint sends out one purple ring (`tryAgainAt`, `hintRingAt`, `.try-again`, `.ring-out`) | `/student` group review | 221, 222, 235 | [architecture/238-try-again-pill.md](architecture/238-try-again-pill.md) |
| 213 | Problem Set 3 — Expanding and factorising as a finished set: `data/pset3/` (ten hand-checked expanding and factorising problems, every written line in the evaluation table, twenty full records equal to the sheet's PS3 column with no sheet change) and its line in `data/finishedSets.ts`; card 19/20, top gap binomial identity (9), monic factorising next (7); the Mistakes tab's slip chips wrap chip by chip, never a word inside a chip | `/teacher`, `/teacher/a/pset-3/*`, `/teacher/a/pset-6/class` (history) | 210, 215 | [architecture/213-problem-set-3-expanding-factorising.md](architecture/213-problem-set-3-expanding-factorising.md) |
| 214 | Problem Set 4 — Non-monic factorising and completing the square is a finished set (`data/pset4/`): ten hand-checked problems, all twenty students' work equal to the class story sheet's PS4 column, card 20/20, 54 mistakes, top gap non-monic factorising (9, graph features next on 8); the sheet's PS4 column gains Q1 as 2x² + 3x − 2, Zara's Q9 and Ruby's Q4 (no status changed) | `/teacher`, `/teacher/a/pset-4/*`, `/teacher/a/pset-5/class` and `/teacher/a/pset-6/class` (history) | 210, 215 | [architecture/214-problem-set-4-nonmonic-completing-square.md](architecture/214-problem-set-4-nonmonic-completing-square.md) |
| 237 | Class View history reads only real earlier sets: a category's stack is the earlier sets that assessed it (up to five, none when none did), no simulated pills; pills read the day alone ("MON 7 SEP"); the class's first set has no "see history"; a pill opens the student's report on that set inside this set's Class View (`?report=&student=&open=`, this set's tabs) with a pulsing "← Return to PSet N" back to the history; stacks sit on today's pill at the tallest open stack's spacing | `/teacher/a/<id>/class` | 215 | [architecture/237-history-real-sets-only.md](architecture/237-history-real-sets-only.md) |
| 217 | Six sets verified end to end: the Classroom (pinned Live, Past PS5 → PS1, before and after Create, short viewports), every set's Class View against the class story sheet with its own New skills, history on Set 6 for all twenty students and every category (exactly the earlier sets that assessed it, dated, each opening that set's report inside Set 6 with Return; no see history on Set 1), Mistakes, Groups and reports on every set, and the live Set 6 demo from Create to End; `lib/setHistory.test.ts`'s skip for unregistered sets removed (the jump test is strict), Create's inference pinned against Sets 5 and 4, the Class View's due line upper-cases a created title; README's six-set Classroom and how to add a finished set | `/teacher`, `/teacher/a/pset-1` … `pset-6` (class, history reports, mistakes, groups, report), `/teacher/assignments/create/review`, `/teacher/board` | 208–216, 234, 237 | [architecture/217-six-sets-verified.md](architecture/217-six-sets-verified.md) |
| 239 | Create's review-pathway map starts on individual working alone (`NEW_SET_PATHWAY`, the demo keeps `DEFAULT_PATHWAY`); hovering or focusing a review stage in the last column shows a short grey line saying what it is (`STAGE_DESCRIPTION`), beside the pill, or under it in the third column where the map has no room | `/teacher/assignments/create/review` | 197, 201 | [architecture/239-pathway-hover.md](architecture/239-pathway-hover.md) |
| 240 | The Mistakes view's Live diagnostic flyout is its problem's step questions on a similar problem (same type, new numbers), stacked and expanded in solution order, each headed by its step name and "n slipped here" and with its own send to class; every distractor mirrors a slip a student really made on the original (`slip`), classmates pick from their own work; make your own removed; inline `$…$` maths in stems (`lib/stem.ts`, `DiagnosticStem`); every option's maths checked in tests (`lib/texEval.ts`) | `/teacher/a/<id>/mistakes`, class card, `/teacher/board`, `/student` | 137, 194 | [architecture/240-diagnostic-step-questions.md](architecture/240-diagnostic-step-questions.md) |
| 246 | Create's review pathway is one line, individual working → individual review → group review → class review → done, in five equal columns: each review stop switched on (accent with ✓) or off (dashed) in place with its description always under it, and a "No review, working only" chip under the line; the pathway starts undecided (`ReviewState.pathway` null, `togglePathway`), and Create waits at 40% with "Choose a review pathway first", a press scrolling to the card and ringing it once; the branching map, its sentence and "continue tomorrow" are gone. | `/teacher/assignments/create/review` | 239 | [architecture/246-pathway-line.md](architecture/246-pathway-line.md) |
| 245 | Every mistake group on the Mistakes view is labelled over its names with the wrong line its students wrote (`MistakeGroup.wrongLines`), in the working's red, across the group's columns, both lines stacked when two, collapsed and open; `FitGrid` fits labels (13–17 px, then widens the group's columns) before the working | `/teacher/a/<set>/mistakes` | 135, 138, 189 | [architecture/245-mistake-labels.md](architecture/245-mistake-labels.md) |
| 243 | The teacher's student report gets the student report's Q tiles under the skills (a column per review stage, ★ on starred problems, faded outside a chosen commentary idea, a line of confidence with practice and caution on the live set) in place of the facts list and Starred card; a tile or skill row opens its working in the skills card's place, the card keeping its size, a problem's versions side by side and only those that tell its story (`shownVersions`); the key moves to the right column's foot; every report fits the laptop; records fill only first try and Incorrect until 244 | `/teacher/report`, `/teacher/a/<set>/report` | 233, 237 | [architecture/243-teacher-report-tiles.md](architecture/243-teacher-report-tiles.md) |
| 241 | A live diagnostic is a chain the teacher paces: step cards in the Mistakes flyout select and clear (tick), `send N to class` runs them in solution order; the board takes over one step at a time ("1st of 3", "14/20 answered", never a count per option), each iPad locks the first tap neutral ("Waiting for the class…"), and the right option turns green on the board and iPads only when all twenty are in or force submit's 10 s countdown closes the step (totals over the responders); one control on the board, the flyout and the class card (force submit → next step → back to work), Withdraw discards the chain, show on board / clear board removed; the class card shows the current step only; the classmates' work stream stands still while a chain is out. Run state in `lib/diagnosticChain.ts`, every close derived from stored moments | `/teacher/a/<id>/mistakes`, class card, `/board`, `/student` | 240 | [architecture/241-diagnostic-chain-paced.md](architecture/241-diagnostic-chain-paced.md) |
| 242 | The Mistakes flyout's result cells show who picked each option: small initials avatars at each cell's foot, one per answer in the order it landed (`pickersAt`, the same arrivals `tally` counts), wrapping inside the cell; a student whose pick is the analogue of their own wrong line on the original wears the slip pill's red, titled "Ethan Kowalski, same slip as on Q1" (`repeatedSlip` against the mistake view's rows, Sam's live row included); the class card's eyebrow names the step's problem ("Q1", "Q1 · 1st of 3"); the class card, board and iPads show no avatars or marks | `/teacher/a/<id>/mistakes`, class card | 240, 241 | [architecture/242-diagnostic-who-picked.md](architecture/242-diagnostic-who-picked.md) |
| 248 | On Create's assessment step, a press on the waiting "Finalise set" (40%, `aria-disabled`) scrolls to the recommendation cards and sends one `.ring-once` purple ring out from each unanswered card, again on every press; "Keep as is" gets an inset ink ring and ink text so it reads as a pill beside Accept | `/teacher/assignments/create/review` | 246 | [architecture/248-finalise-nudge.md](architecture/248-finalise-nudge.md) |
| 247 | Escape closes whatever was opened last, everywhere: one stack of layers (`lib/escape.ts`, bound by `components/useEscape.ts` to a single window keydown), each popup, flyout or expanded view pushing a layer while open with the close its own control does (help cards, prompt → Not now, overlay → Back to Qn, hand-in check → the lowest blank, notice, chat, reopened hints, peers/history → Report, the report's working; Class View drill/column, history mode, history stacks; Mistakes problems most recent first and the diagnostic flyout; the teacher report's working and idea filter; the example menu; Create's tile editor, the Fix box clearing first); focus returns to the opener; the student's quick check is a wall that holds Escape; a press inside it or on the demo controls no longer closes the report's working; a press into a tile's Fix box no longer closes the editor | `/student`, `/teacher/a/<set>/class`, `/mistakes`, `/report`, `/teacher/whole-class`, `/teacher/assignments/create` | 229, 233 | [architecture/247-escape-closes.md](architecture/247-escape-closes.md) |
| 266 | The student report's "← Class view" moves from the right end of the name row to the top left, above the eyebrow, as the Class View's own dark purple "← Edexia Classroom" button (`BackButton` in `AssignmentContext.tsx`): zoomed back from the report's 125% so size, colour and distance under the bar match to the pixel; the name sits flush under the eyebrow; a history report keeps its pulsing Return | `/teacher/a/<set>/report` | 200, 227, 237 | [architecture/266-report-back-button.md](architecture/266-report-back-button.md) |
| 265 | The site's front page is the teacher's Edexia Classroom: `app/page.tsx` is a server `redirect("/teacher")` (a static 307, Back skips `/`); the presenter's chooser moves to `app/demo/page.tsx` with its three cards, the split link and Reset demo, each card's "Open …" line now at the card's foot and the intro naming the set in sentence case; README and the system diagram point at `/demo`; no component linked to `/` (the brand is a label) | `/` → `/teacher`, `/demo` | 184, 186 | [architecture/265-root-is-classroom.md](architecture/265-root-is-classroom.md) |
| 267 | Every back button ("← Edexia Classroom" on Class View, Mistakes, Groups, Create and its review, a set not yet created; the report's "← Class view") sits 21.6 screen px from the window's left edge, where the report's is, whatever column the page centres: `BackButton` takes a CSS `calc` margin (`BACK_LEFT`) over `100vw` and the frame's `--frame-zoom` / `--back-zoom`; the rest of each page stays put | every `/teacher` page with a back button | 200, 266 | [architecture/267-back-button-left.md](architecture/267-back-button-left.md) |
| 250 | A student marked absent on a set (Class View roster: "mark absent" / "mark present" under the name, shown with the row buttons) greys in place, an "absent" pill beside the name, and leaves every count on the set: the Pathway and Mistakes stage counts, n/19 correct and skipped, the Classroom card's submitted, class review's n/m struggled and examples, the diagnostic's n/19 answered and its close, the gate into group review; they leave their seating group for that set's group review only (amber races as three) and show greyed in their seat on its Groups tab. Chloe is absent on Problem Set 6 (`DEMO_ABSENCES`, the story sheet's new absent cell), present on 1–5. The group union takes wrong and started-but-incomplete problems, never unattempted ones | `/teacher/a/<set>/class`, `/mistakes`, `/groups`, `/teacher`, `/teacher/whole-class`, `/board`, `/student` | 185, 189, 241 | [architecture/250-absent-students.md](architecture/250-absent-students.md) |
| 268 | Every back button sits exactly where the report's "← Class view" is on screen: that button lines up with the report's "11 METHODS" eyebrow at every width (at 1512 wide both at 39.6 px) and rises 28 of its px so its top is Class View's from the window (81.6 px, the report's larger bar no longer pushes it down); `BACK_LEFT` now targets the report's column (`max(0, (100vw − 1476px) / 2) + 21.6px`), so "← Edexia Classroom" takes that spot and overhangs its own page's column | every `/teacher` page with a back button | 266, 267 | [architecture/268-back-button-top.md](architecture/268-back-button-top.md) |
| 256 | After the reflection is sent, the student lands on "Your next homework": the Q tiles in set order marked right or wrong beside a Homework folder, over the report's skill dots; one at a time in set order, each problem ever wrong (first submission not holding, a wrong second submission, or not attempted) leaves a dashed slot, grows into its question below the row, its numbers (and any changed words) roll into a hand-authored similar problem's (`data/homework.ts`, same shape so only the numbers move), a line names the type, then it shrinks and arcs into the folder, whose count ticks; derived from `homeworkAt`, so a reload resumes; reduced motion shows the two side by side and no flight; nothing wrong says nothing was added; SKIP TO gains "homework" (`lib/homework.ts`, `HomeworkScreen`, `HomeworkFlight`) | `/student` homework | 233 | [architecture/256-homework-bank.md](architecture/256-homework-bank.md) |
| 251 | A student across every set: `/teacher/students/<id>` and `/teacher/a/<set>/students/<id>` show one page (`HolisticPage`), only Back differs (← Holistic Assessment, ← Class View); the story sheet's line, the category × set grid (a word on each result's colour, not seen hollow, absent grey on cream for a student marked absent on the set (Chloe on PS6, ticket 250), — where a set does not assess the category, `live` on Sam's PS6) over the sets the Classroom holds (PS6 only once created), and the habits behind every result short of secure under their category, a habit worded alike on several sets one row with a "PSn · Q…" ref per set; `lib/holistic.ts` reads finished sets from `data/story.ts` and the live set as its Class View does (`rosterEvidence`, now shared with `TeacherLive`); a column header opens the student's report on that set, a habit's Q opens that working (`?work=`), and the report's back button reads ← Holistic Assessment back to the holistic page (`?from=`, holistic paths only) | `/teacher/students/<id>`, `/teacher/a/<set>/students/<id>`, `/teacher/a/<set>/report` | 210, 237, 243, 250 | [architecture/251-student-across-sets.md](architecture/251-student-across-sets.md) |
| 260 | A sent live diagnostic chain runs in the Mistakes view's focused view (`DiagnosticFocus`) in place of the problems, which stay mounted but hidden: the header "Q1 x² − 5x + 6 = 0 · Live diagnostic", every step of the chain side by side in solution order in a centred row (asked: results and avatars; current: accent border, live; later: question and options at 50%), cards narrowing equally to fit one row (`flex: 0 1 460px`, `@max-[300px]` stacks options and the step heading, `FitStem` fits a stem's widest maths), and under the row a band held at the window's foot with the position, the answered count, the one control and Withdraw; done or a withdraw brings the page back at its scroll (`useScrollAroundFocus`). The flyout's open state and selection move to a module store (`app/teacher/diagnosticFlyout.ts`) so a remount (Fast Refresh on the demo's `next dev`, the collapse the user saw) keeps them; `DiagnosticStep` shares the step pieces; force submit counts 5 s; the control reads next question and done everywhere | `/teacher/a/<id>/mistakes`, class card, `/board` | 189, 241, 242, 250 | [architecture/260-diagnostic-focus-view.md](architecture/260-diagnostic-focus-view.md) |
| 264 | `/student` is Sam's Edexia Classroom on the iPad: To do, Missing, Completed (`lib/studentClassroom.ts`), PS1–PS5 Completed from the story, Problem Set 6 in To do only once sent (Create, a skip or a deep link; a fresh demo has not sent it), Completed once the report goes with its reflection, Missing when the lesson ends without his hand-in; START / CONTINUE opens `/student/a/pset-6`, the Edexia mark returns; `app/student/layout.tsx` keeps the iPad (`StudentShell`: the lesson's clockwork, countdown, diagnostic, SKIP TO) mounted across both; `/student?stage=…` redirects and sends the set first (`deepLinkClassroom`); Create resets Sam's run to its start; the blank board names no set before one is sent | `/student`, `/student/a/pset-6`, `/board` | 188, 256, 265 | [architecture/264-sam-classroom.md](architecture/264-sam-classroom.md) |
| 244 | Every record on every set (all twenty students, Sam's finished records included) carries what review made of its mistakes: each problem fixed on the student's own rework has its second submission, and every wrong problem its seating group's one version (the rework that checked, or the last try, the first habit-holder's working; sky's Set 6 versions are the demo group's script), written by `withReview` from `data/psetN/review.ts` and `data/classmates-review.ts`; the class story sheet gains a review part per set (`STORY_REVIEW`, every case's reasoning) held to the agreed rules by `lib/reviewRule.ts`; on the live set a classmate's second submission shows once individual review is finished and the group's version once group review is (`recordReviews(…, over)`, `reviewStagesOver`); on the teacher's report the not-solved note is Incorrect's second label line, the skills card takes the page's spare height and a working too tall for it scales down whole, so every working opens without scrolling | `/teacher/a/<set>/report` | 243 | [architecture/244-record-review-outcomes.md](architecture/244-record-review-outcomes.md) |
| 253 | On a set's Class View a student's name, their avatar and the closing avatar open their holistic page under the set (`/teacher/a/<set>/students/<id>`, Back → that Class View); same boxes, nothing moves; in another student's history mode the press leaves history mode instead; "student report" and "mark absent" stay. A "Did you know? A name opens that student's Holistic Assessment, also in Edexia Classroom." note with Dismiss lies over the Student head's blank space beside its label (absolute, in the sticky head); Dismiss is for good in this browser (`lib/holisticNote.ts`, its own localStorage key read through `useSyncExternalStore`, untouched by Reset demo; clearing the key shows it again) | `/teacher/a/<set>/class` | 250, 251 | [architecture/253-name-opens-holistic.md](architecture/253-name-opens-holistic.md) |
| 252 | Holistic Assessment: an entry on Edexia Classroom's title row beside "+ New assignment" (one height, the Live cards unmoved) opens `/teacher/students`, twenty tiles three to a row, one height per row; a tile shows avatar, name, the story sheet's line, the habits seen on two sets or more as "label · n sets" under their category (a habit the sheet words differently by set collapses by an authored label in `data/habitTags.ts`, e.g. Sam's "signs in the wrong brackets · 2 sets") and the strengths, the categories secure on every set that assessed the student in them; `lib/holisticTiles.ts` over `holisticView` (ticket 251), so the live set moves the tiles as it moves the student's page; a tile opens `/teacher/students/<id>`, whose Back and the browser's back return at the scroll left (sessionStorage) | `/teacher`, `/teacher/students` | 251 | [architecture/252-holistic-tiles.md](architecture/252-holistic-tiles.md) |
| 270 | On a set's Class View the "mark absent" toggle is disabled for a student who has handed the set in (`absenceLocked` in `lib/absence.ts` over `rosterProgress`, ticket 185's one meaning of submitted): same place and width, grey, no hover, a "has handed this set in" tooltip; on the live set it disables as the stream hands a classmate in; "mark present" always acts, so a mark can be undone | `/teacher/a/<set>/class` | 250, 185 | [architecture/270-absent-locked-once-submitted.md](architecture/270-absent-locked-once-submitted.md) |
| 269 | The holistic page's grid turns: a row per set (PS1–PS6, each row head the set's label, day, topic and live pill, still opening its report) and a column per category, each headed by the Class View roster's light blue uppercase chip, now one `CategoryChip` (`components/Tag.tsx`) shared by both, level with the SET corner; `HolisticView.columns`/`rows` become `sets`/`categories` | `/teacher/students/<id>`, `/teacher/a/<set>/students/<id>`, `/teacher/a/<set>/class` | 251, 252 | [architecture/269-holistic-sets-as-rows.md](architecture/269-holistic-sets-as-rows.md) |
| 271 | Every problem a teacher screen names shows the whole question, the stem's words then the expression, never the bare expression (`components/ProblemQuestion.tsx`: one wrapping line, the maths never splits, a hyphenated word never breaks via `unbrokenHyphens`, a figure follows as a small thumbnail): the Mistakes header, the report's open problem, the skill working (`ProblemWork`, the student's report too), the live diagnostic view (header "Qn · Live diagnostic" over the question), class review setup's list and example cards, board controls and Before and after | `/teacher/a/<id>/mistakes`, `/teacher/a/<id>/report`, `/teacher/whole-class`, `/teacher/board`, `/teacher/compare`, `/student` report | 244, 260 | [architecture/271-whole-question.md](architecture/271-whole-question.md) |
| 263 | The teacher's presenter SKIP TO (`app/teacher/TeacherSkipTo.tsx`) in a strip of its own under the scroll region (`TeacherChrome`, Reset demo beside it): send assignment (PS6 live now under the demo pathway, Sam at his start), students done with current stage (the next stage of the pathway in force from `currentClassStage`, Sam with his scripted work; disabled before a set is sent; the last stage completes), activity completed (every stage over, `lessonEndedAt`, report and reflection in: PS6 in Past and Sam's Completed); `teacherSkip` in `lib/demo.ts` is a pure step from the demo as it stands (pathway, seating, absences, a class review setup kept); `setLesson` moves the classroom and the session as one change so the iPad's clockwork never sees half a jump; a sent set starts a new lesson (`assignment/create`); `lessonOver` read by the stage, the board, the group card and the Class View | `/teacher/*`, `/board`, `/student`, `/student/a/pset-6` | 250, 256, 264 | [architecture/263-teacher-skip-to.md](architecture/263-teacher-skip-to.md) |
| 279 | On a set's Class View "see history" stands every category's earlier results at once (`historyCategories` in `lib/setHistory.ts`: the shown categories with an earlier result), rather than waiting for a press on each category pill; a pill still toggles its own stack, one Escape or "close history" closes it all, and the way back from a history pill's report (`?history=<student>`, the `open` query removed) reopens every stack | `/teacher/a/<set>/class` | 175, 215, 237, 247 | [architecture/279-history-opens-all.md](architecture/279-history-opens-all.md) |
| 274 | The homework sequence runs at 2 s a problem (was 5.6 s): expand 250 ms, the original 250, the numbers rolling into the similar problem's 650 (the longest phase), the similar question 350 with its line fading in as the change settles, fly 450, gap 50; Sam's five land by 11.15 s after Send (was 29.15 s); reduced motion shows each pair side by side for 1950 ms on the same 2 s clock; the Homework folder shows no count anywhere (badge, label, attribute) and bumps as each tile lands (`lastLanded` in `lib/homework.ts` replaces `bankedCount`) | `/student/a/pset-6` homework | 256 | [architecture/274-homework-faster.md](architecture/274-homework-faster.md) |
| 277 | A student's holistic page: a coloured result opens a flyout under its row, over the rows below (the category's whole `SkillTree` on that set, every group open, grey "click to see examples" with an arrow to each skill; `CellDrill` in `app/teacher/students/HolisticDrill.tsx`), read from `holisticWork` (`lib/holistic.ts`, the Class View's evidence, so the tree rolls up to the result); a skill puts its problems (`SkillWork`, `ProblemWork` cards, `problemsBehindLeaf` in `lib/hierarchy.ts`, which also fills Class View's empty working panel) in a side column in the habits' place; the habits run down that 540 px column beside header and grid, zoomed to the scroll region's height (`components/FitHeight.tsx`), so a laptop never scrolls; sets newest first, grid slightly smaller. |
| 275 | The Class View's "did you know?" note reads "Did you know? Clicking on a name opens that student's Holistic Assessment." (Dismiss and its stored dismissal unchanged, still an overlay starting where it did over the Student head), in two balanced lines with its text narrowed to the wider line (a layout effect in `HolisticNote`), so no blank run before Dismiss; the live student's row loses the "Report →" link under his name (shown once his report was sent, to the old `/teacher/report`), so his report is the row's "student report" button like every other student's; his pill shows only while he is on the set (`liveStudentTag` in `lib/progress.ts`: "Qn in progress", "warming up", "not started"), none once handed in; every other roster rect unchanged, Sam's name centres in his row | `/teacher/a/<set>/class` | 253 | [architecture/275-class-view-roster-tidy.md](architecture/275-class-view-roster-tidy.md) |
| 280 | On a set's Class View another student's skill tree closes like history mode: a click on another row's empty space or cells only closes it (`leaveDrill`), the next opens that row's full tree; that row's pills, see dot skills and see history act at once; a category pill opens the category's full tree, dot skills included, and a row tap the whole full tree (both were double-clicks, now removed; `repeated` swallows a double-click's second click); the pressed row stays still on screen as a tree above it opens or closes (`anchorRow`) | `/teacher/a/<set>/class` | 128, 133, 175, 279 | [architecture/280-drill-closes-on-other-row.md](architecture/280-drill-closes-on-other-row.md) |
| 276 | Habits become **patterns** in the UI and the code (`data/patternTags.ts`, `Pattern`, the review rule's `pattern` basis, `data-pattern-*`); one rule in `lib/holistic.ts` decides what surfaces on the student's page and the tiles alike: `recentSets` takes the class's latest five sets by due date and `surfacing` keeps a pattern (a tag's wordings) with an occurrence among them, with every earlier set it occurred on (PS2–PS6 once PS6 is sent, so ten PS1-only patterns leave; PS1–PS5 before); tiles show one-set patterns ("· 1 set"); the page drops its Habits header and the live pill on Sam's live set | `/teacher/students`, `/teacher/students/<id>`, `/teacher/a/<set>/students/<id>` | 251, 252, 269 | [architecture/276-patterns.md](architecture/276-patterns.md) |
| 278 | Group review takes every problem a member did not get right first time: a mistake, one started and left incomplete, and one not attempted (`reviewProblemsOf`, `recordReviewProblems` in `lib/group.ts`, replacing ticket 250's rule; absent members still out), so the board, its intro ("made a mistake on or didn't finish"), standings, the teacher's group card, the projector's leaderboard, the class stage's done count and the skip fixtures all follow; Sam's live group works Q1–Q10 (Q4, Q5, Q6, Q8 added because Liam, and Jordan on Q8, never reached them: the right working holds first time; Q9, which nobody had right, scripted by ticket 281's single-line exception: Zara's slip twice, the hint naming it, then the height), every added problem with a pen in `DEMO_PENS` (Liam Q4, Jordan Q5 and Q8, Zara Q6); `RACE_SCHEDULE` re-timed (mint 4:15, amber 4:25, coral 10:10, violet 11:00 around the demo group's quickest 5:40), the report jump's run 12 to 5 minutes ago; `DEFAULT_PATHWAY` individual → group → class review; tickets 281 and 282 written | `/student/a/pset-6`, `/board`, `/teacher/a/pset-6/class`, `/teacher/whole-class`, `/teacher/board` | 250, 244, 221, 222, 228 | [architecture/278-group-review-surfaces-all.md](architecture/278-group-review-surfaces-all.md) |
| 283 | The holistic grid's right margin equals its left: an empty 16 px gutter column after the last category (`GUTTER` in `app/teacher/students/HolisticPage.tsx`), and room for the Class View's widest chip (COMMUNICATION, 126 px) inside its column by narrowing the side column to 500 px and the set column to 256 px (category columns 129.5 px). |
| 272 | The teacher's presenter "send assignment" opens Create's last step filled in, not sent (`teacherSkip("send")` in `lib/demo.ts`: `unsent` in `lib/classroom.ts` plus `readyDraft`, Generate's set with every recommendation accepted, the demo pathway chosen, New skills inferred; the bar pushes `REVIEW_ASSIGNMENT_HREF`); Create there sends through `lib/create.ts` (`createAction`, lifted out of `ReviewAssignment`) and lands on PS6's Mistakes; students done and activity completed open the set on Sam's iPad from his Classroom (`subscribeLessonMoves` in `lib/store.ts`, read by `StudentClassroom`); a move with nothing out (send, Reset) leaves him there ; added the same day: an accepted addition takes a removed question's slot (`applyReview` in `lib/review.ts`, so the step lists PS6 in the students' order, the ball problem at Q9, and its card reads "added as Q9"), and every Create step leaves the floating bar's height under its content (`CREATE_BAR`, `CREATE_BAR_CLEARANCE` in `app/teacher/assignments/create/createBar.ts`) | `/teacher` bar, `/teacher/assignments/create/review`, `/student` | 263, 264, 246, 188 | [architecture/272-skip-send-opens-create.md](architecture/272-skip-send-opens-create.md) |
| 284 | On a set's Class View a student's open skill tree is a sheet over the rows below their row (`DrillSheet` beside `HistoryBlocker` in the roster box), no longer a row pushing every student down: nothing moves as a tree opens, closes or swaps; the sheet spans the table from the row's bottom edge, ends on the next row line (or past the last row, the roster box padding so it scrolls into view), closes on a click on its blank paper, and keeps the student's row buttons in view while hovered; the column view still opens rows | `/teacher/a/<set>/class` | 175, 279, 280 | [architecture/284-skill-tree-overlay.md](architecture/284-skill-tree-overlay.md) |
| 285 | The Class View's Set column is the score on the first submission (`lib/setScore.ts`): problems finished with no wrong line over the set's problems, never lifted by a second submission, rework or the group's version (a record: inside `done` and off `wrong`; the live student: his `lines` and typed answer, never `rework`); a dash while a student is still on the set, missing and absent unchanged; the live student's "handed in" line gone. Problem Set 5 read handed-in counts (Sam 10/10 with three wrong), now Sam 7/10, Tomas 3/10, Grace 7/10. |
| 273 | On a set's Class View the Pathway card offers "end lesson" once the class is on the pathway's last stage and it is not class review (`endsLesson`, `canEndLesson` in `lib/classStage.ts`): force submit's pill laid above force submit (`app/teacher/EndLesson.tsx`), nothing on the card moving; a press starts the one-minute grace (`end-lesson` advance), the card shows "ending lesson", how many are not done and the countdown with Cancel over force submit and its count, the iPad reads "Your teacher is ending the lesson in"; at the deadline every student still in the lesson lands on the report with their work as it stands (`lib/session.ts`) and `lesson/end` stamps `lessonEndedAt` at the deadline and ends a group run (`lib/classroom.ts`), from the iPad and from every teacher tab (`LessonEnds` in `TeacherChrome`): the set in Past, the board blank | `/teacher/a/<set>/class`, `/student/a/pset-6`, `/teacher` | 263, 145, 272 | [architecture/273-end-lesson.md](architecture/273-end-lesson.md) |
| 281 | The class data follows the realistic group rules (`lib/reviewRule.ts`): a member brings every problem not right first time; a one-off slip is fixed on their own rework; the group solves the rest when a present member had it right first time and otherwise closes it on its own last try (never a member's first submission), with at most one declared exception a set (Set 6's Q9 at sky); Liam hands in five on every set (statuses unchanged where the sheet saw his work; Sets 3 and 5 now read results); one or two groups a set meet the hardest problem with nobody able to do it (violet Q10 on Set 1, mint's Q10 on Sets 2–6 and Q9 on Set 5); class review on Sets 1, 3 and 6 records the covered problems and one or two real wrong first submissions each (`ClassReview`, `classReviewFrom`); Sam's board drops Q4 and scripts Liam's Q5; review data and the sheet's reasons regenerated, held together by `reviewMismatches`, `classReviewMismatches`, `hardestUnsolved` | `architecture/281-class-data-group-rules.md` |
| 282 | Both reports sort problems into five columns: Correct first try needs a finished first submission (`firstFinished`, `recordFinished` in `lib/setScore.ts`; Sam's unfinished Q9 left that column); a new grey **Covered in class review** (after group review, before Incorrect) holds a problem the student's group left unsolved that class review showed, from a finished set's record or, on the live set, the board once class review has ended (`WholeClassSession.reached`, `boardCovered`, `liveClassReview`, `reportPathway`), its working ending in a Class review pane of the board's examples, lettered, unmarked and anonymous (`components/ClassReviewExamples.tsx`); a not-attempted problem goes where its group's version puts it, its first pane reading *not attempted*, and each column names its not-attempted problems; the not-solved note and tag are gone; Sam's side column shows the same versions stacked; every teacher report fits 1280×800 again (the presenter strip at `TEACHER_ZOOM` on every page, the report's bottom padding 16 px via `TeacherChrome fill`) | `architecture/282-report-not-attempted-class-review.md` |
| 286 | The site's front page is the presenter's chooser again: `app/page.tsx` renders the chooser (Student, Teacher and Smartboard cards, the one-tab split link, Reset demo), moved back from `app/demo/page.tsx`, which now redirects `/demo` to `/` (a static 307); reverses ticket 265's `/` → `/teacher`; README and the system diagram name `/` | `architecture/286-root-is-chooser.md` |
| 288 | The create strip reads QUESTIONS — DIFFICULTY — REFINE — PATHWAY — SEND: the steps are data per kind of set (`PIPELINES` in `lib/createPipeline.ts`, `pset` only; ticket 291 adds homework without Pathway), `Steps` renders the list it is given, `currentStep` picks the lit step; Refine is the label of the internal `assessment` step; Send is never a page: Create lights it for `SEND_LIGHT_MS` (600 ms) with the strip locked and Back and a second Create ignored, then sends and lands on the set as before, the page keeping the sent draft on screen until the route changes (no "Nothing drafted yet" flash, which the old Create showed for a frame); the Classroom button reads "+In-Class PSet" (the not-created card's link too) and the review screens' eyebrow drops "new assignment" | `architecture/288-create-strip-refine-send.md` |
| 287 | A Completed card on Sam's iPad Classroom opens his read-only report on the set: the whole card is one press target (hover lifts it, press settles it; no OPEN button, so ticket 290's homework cells can sit to its right), `href` on `StudentSetCard` (`studentReportHref`, `/student/a/<id>/report`), Missing cards still open nothing and To do keeps its action; the report (`app/student/CompletedReport.tsx`, data `studentReport` in `lib/studentReport.ts`) is the end-of-lesson report's layout, split out as `app/student/screens/ReportLayout.tsx` and shared with `ReportScreen`: PS1–PS5 from his handed-in record with every review stage over and the recorded class review, PS6 from his session once the report is sent (the homework folder still plays at `/student/a/pset-6`), the same columns, tiles and working as the teacher's report of Sam; his reflection is the record's `clarification` (PS1 gains one) or his sent text, shown where the box was, with "Sent to Ms Okafor"; "← Classroom" or Escape goes back; a deep link survives reload; the lesson still pulls him to the live set from here (`useLessonPull`, shared with the Classroom); What happened's columns keep their tiles on one row only while the card has room (`outcomeTemplate` with `rows` and `gap`), which fixes PS1's ten right-first-time tiles pushing Incorrect 21 px out of the card | `architecture/287-completed-set-opens-report.md` |
| 289 | Create's Questions page has a due-date picker beside the title (`components/DuePicker.tsx`, shared; ticket 291 reuses it with its own earliest day and a note): the field reads "DUE · Thu 10 Sep" and opens a month calendar as an overlay under its right edge, moving nothing; days before the demo's today (Thu 10 Sep 2026, `DEMO_TODAY`) are greyed and cannot be chosen; arrows, Home/End, Page Up/Down, Enter/Space, Escape and a press outside work. The day is stored as a calendar day (`IsoDay`, `lib/dueDate.ts`) on the draft and on the created assignment and read as the live set's `due` ("Thu 17 Sep") by the teacher's and Sam's cards, their sorting, the Class View and Sam's set overview; presenter skips and deep links still send Problem Set 6 due Thu 10 Sep | `architecture/289-due-date-picker.md` |
| 296 | A dev-only design tuner (⌥C under `next dev`, `components/DesignTuner.tsx`): every colour family in `app/globals.css` by lightness, vividness and hue (OKLCH, `lib/oklch.ts`), shades following their main, red one control over gap + wrong until split; the status markers' pill and dot corners and the incomplete fill (split at any line, corner to corner, or hatched) as new `--marker-*` tokens read by `.marker-half`, `StatusDot`, `SkillColumns` and `StatusKey`; one scale over Tailwind's corner steps; Space held shows the saved design, ⌘Z undoes, ⌥-click finds an element's colours; Save rewrites only the changed values through `app/api/dev/design-tokens` (dev only, 404 in production, file found beside the route); the layout imports the tuner only in development; markers pixel-identical at the defaults | `architecture/296-design-tuner.md` |
| 298 | Student profiles describe behaviour, never traits: all 20 Holistic Assessment summaries (`STORY[id].arc` in `data/story.ts`) say what the work shows, with no careless, confident / low in confidence, slow, rushes, guesses or unsure; Sam's names the one sign pattern his seven patterns share across five topics (a minus belongs to the term that follows it); pattern wordings, review reasons and tile tags (`data/patternTags.ts`) are rewritten the same way ("in a rush", "guessed", "jumped straight to it", "astray"), the records' notes in `data/classmates.ts` and `data/pset1–5/classmates.ts` equal them, students' own reflections unchanged; `data/story.test.ts` fails on a trait word in any summary, pattern, tag or review reason; `specs/class-story.md` regenerated | `architecture/298-behaviour-not-traits.md` |
| 299 | Mistakes are named by misconception, from one taxonomy with permanent ids: `data/misconceptions.ts` (37 misconceptions, each a name of five words or fewer and an `about`; ids never renamed or reused) and every wrong line of the six evaluation tables points at one (`LineVerdict.misconception`, `name` removed); the Mistakes tab's pills name and cluster by it (`groupBySlip`, `SlipChip`), a long pill widening its columns in `FitGrid`; the Classroom's top gap, the class review picker's options, the repeated-slip rule (`lib/reviewRule.ts`) and the class story checks use the same id; `lib/misconceptionCounts.ts` counts sightings, students, sets and classes by id; 14 past review cases became repeated slips and the sheet, records and `specs/class-story.md` follow | `architecture/299-misconception-taxonomy.md` |
| 301 | Work lines and Compare name the misconception, not the skill: `MisconceptionChip` (`components/Tag.tsx`, ⚠ + taxonomy name, a span) on every red line of `WorkLines` (the Class tab's drills, the teacher's and Sam's reports, the holistic page's skill work) and on Compare's handed-in red lines, lines that hold carrying only their label; the chip's old skill link is removed end to end (`onGoTo`, `RowDrill.onNavigate`, the drill's and holistic page's `goTo`, TeacherLive's `jump`); the class review picker's skill chip and `ExampleOption.leaf` go | `architecture/301-misconception-chips.md` |
| 302 | A diagnostic's distractors point at the misconception taxonomy: `DiagnosticOption.misconception` is a `MisconceptionId` (a slip-tied option's is its wrong line's) and its old text is `detail`; seven entries join `data/misconceptions.ts` for distractors no student wrote; on the teacher's laptop an option reads the misconception's name with its detail beneath (two options under one misconception still read apart), the board neither; `diagnosticSightings` counts a chain's picks as sightings (`source: "diagnostic"`) | `architecture/302-diagnostic-misconceptions.md` |
| 290 | Homework becomes a kind of assignment and Sam's iPad Classroom shows a homework column to the right of his Completed cards: `data/homeworks.ts` (Homework 1 due Tue 1 Sep, Homework 2 due Mon 7 Sep; `SAM_HOMEWORK_STORY`, his named simulation data: HW1 done on time, HW2 missed; `DEMO_TODAY` Thu 10 Sep from ticket 289) and `lib/homeworks.ts` (pure: `homeworkForDue`, the date rule, a homework covers the sets due on or after the previous homework's due date and before its own, so HW1 = PS1–2 and HW2 = PS3–4; `homeworkStatus`, completed / missed, final / open; `homeworkColumn`, each homework one cell with row and span over its Completed sets, an empty space beside a set no homework covers yet, PS5 and PS6 once Completed); `StudentClassroom` widens its column to 1066 px so cards keep 780 and every section's cards sit in a `[1fr, 190px]` grid, the cells gridded to span their rows exactly: a green check with "HW1 completed", the Class View's caution triangle with "HW2" and a grey "problems added to next HW"; not pressable; the triangle extracted to `components/CautionTriangle.tsx` (Class View pixel-identical). See `architecture/290-classroom-hw-column.md`. |
| 297 | "Not now" keeps the practice offer armed: during individual working a second mistake on a topic (taxonomy group) still offers isolated practice, but the count now resets only when practice is taken (`practiceTaken` in `lib/escalation.ts`, called by `prompt/accept` in `lib/session.ts` and by `requestHelp`), so after "Not now" the very next slip on that topic offers again, on the most fundamental leaf slipped since practice was last taken; a slip on another topic offers nothing; after Yes the next slip is a first mistake; entries and caution still count offers; the offer says which mistake it is (`promptSentence`: second, third…). Reproduced in the browser first (the offer did fire on Q2 line 2; the simulated student's "nothing stopped me" came from the reset on decline and from counting by kind of error, where the user kept topic). vitest 1031, eslint, tsc, next build; click297.mjs 42/42 | `architecture/297-mistake-tripwire.md` |
| 291 | +Homework creates and sends Homework 3: the create flow takes a kind (`CreateKind` `pset` / `homework`, `CREATE_ROUTES`, `PIPELINES.homework` = QUESTIONS — DIFFICULTY — REFINE — SEND, no Pathway), each with its own draft and review in the classroom (`homeworkDraft`, `homeworkReview`); `/teacher/homework/create` and `/review` reuse `CreateAssignment` and `ReviewAssignment`; Generate fills Homework 3 (`data/homework-draft-seed.ts`: ten new problems on features of a parabola and roots of a quadratic, Q8 with no x-intercepts and Q9 a repeat of Q3 seeded for Refine's `HOMEWORK_RECOMMENDATIONS`), no goal box; the picker starts at Mon 14 Sep and blocks every day on or before Mon 7 Sep (`nextHomework`); Refine's button is Create, which lights Send and dispatches `homework/send` (`SentHomework` in `classroom.homeworks`, idempotent, no lesson, no session reset) and lands on the Classroom; `classHomeworks(c)` is the fixtures' Homework 1–2 plus sent ones, so Homework 3 covers Problem Sets 5 and 6 by the date rule; an in-class set's picker notes "Mistakes from this set go into Homework N" for a date inside an opened homework (`psetDueNote`, `openedAt` for ticket 292), out of the flow; the teacher's Classroom has `+Homework` and homework cards among Past (`homeworkCards`, `pastWithHomework`: sent / open / Sam's check or caution, covered sets, nothing to press); reloading the Questions page no longer focuses the ghost tile and scrolls | `/teacher`, `/teacher/homework/create`, `/teacher/homework/create/review`, `/teacher/assignments/create` | 288, 289, 290 | [architecture/291-homework-create-send.md](architecture/291-homework-create-send.md) |
| 292 | Homework 3 waits in a Future panel on Sam's iPad Classroom and opens when the last lesson among its sets ends: `openHomeworks(c)` (`lib/homeworks.ts`, pure, idempotent) stamps a sent homework's `openedAt` (the lesson's `lessonEndedAt`, never before `sentAt`, so every tab stamps alike) and freezes `setIds` once every set it covers has its lesson over (`homeworkSets`: the Classroom's sets plus Problem Set 6 even before its Create; the live set's lesson over by `lessonOver`); the store applies it on every write (`setClassroom`, `setLesson`, `adoptClassroom`) and every reader goes through it (`classHomeworks`), so the teacher's end lesson after its minute, the presenter's activity completed and class review's End all open it, live in every tab; `homeworkForSet` puts a set into the homework that froze it, else by date passing over homeworks that opened without it (a set created later goes to the next); the fixtures' Homework 1 and 2 carry frozen `setIds`; `futureHomeworks` (the panel: name, due, "opens after Problem Set 6"), `openHomeworksFor` (To do's first cards: open and neither completed nor missed), `missedNote` ("problems added to current HW" once the next homework is open); `StudentClassroom` adds `FuturePanel` (dashed, grey, no shadow, absolute top right over the homework column, level with the eyebrow, not pressable, hidden when nothing is scheduled), the Homework card first in To do with OPEN (`StudentSetCard.kind`), the HW3 cell muted and inert while in the Future and a press target once open, both to `/student/homework/<id>` (`HomeworkScreen`: heading and ← Classroom, ticket 293 fills it) | `lib/homeworks.ts`, `data/homeworks.ts`, `lib/classroom.ts`, `lib/classroom-store.ts`, `lib/store.ts`, `lib/studentClassroom.ts`, `app/student/StudentClassroom.tsx`, `app/student/HomeworkScreen.tsx`, `app/student/homework/[id]/page.tsx`, `lib/homeworkGate.test.ts` | [292](architecture/292-future-panel-homework-gate.md) |
| 300 | A student who names one kind of factorising practises that kind: `focusLeaves` (`lib/warmup.ts`) drops the other kind when factorising was ticked as both and the chat's words name only one, and `NONMONIC_SAID` reads "the coefficient in front of the x²", "leading coefficient" and "a isn't 1" as non-monic; every practice skill in `data/practice.ts` has exactly one follow-up after its worked example (fourteen new, monic's already there), in the warm-up and the mid-set practice alike; the hint-box sweep walks the follow-ups too (`scripts/warmup-leaves.json` `[first, followUp]`); the sketch's closing step shortened to fit its column | `/student/a/pset-6` warm-up and mid-set practice | 27, 48, 104, 199 | [architecture/300-practice-follow-ups.md](architecture/300-practice-follow-ups.md) |
| 295 | The presenter's homework jumps, shown only once the teacher has pressed +Homework in this demo: `homework/start` stamps `ClassroomState.homeworkStartedAt` (simulation only; the first press counts, Reset demo clears it, a new lesson, the teacher's jumps and Sam's rebuilds keep it via `keepHomeworkStarted`); `homeworkSkipsShown` adds "send homework" and "homework open" after the existing buttons on the teacher's presenter strip and Sam's SKIP TO (`HOMEWORK_SKIP_TARGETS`, `homeworkSkip` in `lib/demo.ts`, pure steps from the demo as it stands); send homework is +Homework's real Create without walking it (`readyHomework`: Generate's ten with every Refine recommendation accepted, due Mon 14 Sep, through `homeworkSent`; any sent homework replaced, so it is always Homework 3), written with `setClassroom` so no screen answers it as a lesson move (Future panel live on Sam's Classroom; the teacher lands on `/teacher`); homework open sends Homework 3 if none is sent, then brings Problem Set 6's lesson to its end as activity completed does (`completeLesson`), written with `setLesson` and `Lesson.land = "classroom"`: `useLessonPull` opens no set for it and `useLessonLanding` (in `StudentShell`) takes Sam's iPad to his Classroom from any student route, where Homework 3 is first in To do, its cell open, HW2's note "current HW" | `lib/demo.ts`, `lib/classroom.ts`, `lib/store.ts`, `components/homeworkJump.ts`, `components/SkipTo.tsx`, `app/teacher/TeacherSkipTo.tsx`, `app/teacher/Classroom.tsx`, `app/student/StudentShell.tsx`, `app/student/useLessonLanding.ts`, `app/student/useLessonPull.ts`, `lib/homeworkSkips.test.ts` | [295](architecture/295-homework-presenter-skips.md) |
| 293 | Sam's homework screen lists his Homework 3: `homeworkList(id, classroom, session)` (`lib/homeworkList.ts`, pure; null unless the homework is open for him) is a pipeline ticket 294 extends, `ownSets` (the frozen sets, newest first) → `ownProblems` (each set's ever-wrong problems, `everWrongOn`: a finished set's handed-in record, Problem Set 6's session as it stands, the folder animation's rule; each with `similarFor`) → `groupBySet` (empty sets omitted), then Everyone (the sent homework's ten as Refine left them), numbered on from 1; `data/homework-similar-ps5.ts` holds Problem Set 5's Q4, Q6, Q9 similar problems (original's TeX shape, stem and skills); `app/student/HomeworkScreen.tsx` shows FROM YOUR MISTAKES with a card per set under its name, then EVERYONE, each row a number and the whole question (`components/ProblemQuestion`, which now sets a typed stem's inline `$…$` maths, a question with no expression and an uploaded diagram; a figure a 72 px thumbnail), nothing pressable but ← Classroom / Escape | `lib/homeworkList.ts`, `data/homework-similar-ps5.ts`, `lib/homework.ts`, `components/ProblemQuestion.tsx`, `app/student/HomeworkScreen.tsx`, `lib/homeworkList.test.ts` | [293](architecture/293-homework-screen.md) |
| 303 | Error signatures across sets on Holistic Assessment: every story pattern names the misconception its student's wrong lines show (`Pattern.misconception` in `data/story.ts`, held to the ink by `data/story.test.ts`), `data/signatures.ts` groups the 37 misconceptions into 14 families (Minus signs wrong, Factor pairs wrong, …, `steps` for communication), and `lib/signatures.ts` makes a family on two sets or more a signature from the patterns the page surfaces; the page shows them as an "ACROSS SETS" chip line under the summary (a lit chip rings its cells, marks its patterns and opens a flyout with what its lines look like, moving nothing; no page scroll on a laptop fresh or after PS6), the tiles as the first tag group with covered patterns out of the category groups; pattern wordings, review reasons, tags and summaries say what is wrong, never why (no guessed, not checked, copied, tried); Sam's seven sign patterns read as Minus signs wrong on PS2–PS5 | `architecture/303-cross-set-signatures.md` |
| 294 | A missed homework's leftovers carry into Sam's Homework 3 without duplicates: `lib/homeworkList.ts` gains `missedBefore` → `leftovers` (the missed homework's own ever-wrong problems, never its teacher's ten) → `carryOver` (a leftover whose one skill, `primarySkill` in `lib/problemSkill.ts`, this homework's own problems or a newer leftover already hold is dropped) before `groupBySet`; `missedNote` moved there and takes the session (no note when nothing carried); `data/problem-skills.ts` tags Problem Sets 5–6 (Sets 1–4 read the story outline's first leaf); `data/homework-similar-ps4.ts` and `-ps3.ts` hold every leftover's similar problem; Sam's Problem Set 4 gains Q10 (signs in the wrong brackets) in his record, the evaluation table and the story sheet | [architecture/294-missed-homework-carries-over.md](architecture/294-missed-homework-carries-over.md) |
| 304 | Every diagnostic distractor says what picking it means: `DiagnosticOption.ifChosen` on all 105 distractors (the rest of "If you chose A, you…", inline `$` maths, what the choice gives and never why), none on a right option; on the board, once the step is revealed, each wrong option's cell reads "If you chose A," over "you…" scaled to one row (laid out invisibly from the push, so the reveal moves nothing); on the student's iPad the status line under the options reads "You chose A, meaning you…" or "You chose C, correct." after the reveal only, one line, the green fill on the right answer and no mark on a wrong pick; `proseParts` (`lib/stem.ts`) and `components/MathProse.tsx` draw words with inline maths for both the stem and the lines; teacher surfaces unchanged | [architecture/304-diagnostic-if-you-chose.md](architecture/304-diagnostic-if-you-chose.md) |
| 306 | A missed homework's cell on Sam's Classroom has a dark red border: `HomeworkColumn` (`app/student/StudentClassroom.tsx`) draws the missed cell with `border-wrong-deep` instead of `border-line`, still 1 px, so nothing moves | [architecture/306-missed-hw-red-border.md](architecture/306-missed-hw-red-border.md) |
| 305 | The teacher's Classroom shows homework as a column beside Past, not as cards in the list: `teacherHomeworkColumn(past, c)` (`lib/classroomCards.ts`, replacing ticket 291's `homeworkCards` / `pastWithHomework`) runs ticket 290's `homeworkColumn` over the Past cards and the class's homeworks, so a cell spans exactly its covered sets' rows (HW1 beside PS1–PS2, HW2 beside PS3–PS4, HW3 beside PS5 while PS6 is Live and PS5+PS6 once it is Past) and a Past set no homework covers keeps an empty space; each cell reads the class, never Sam: `sent` (dashed, "sent · opens after Problem Set 6" from `futureHomeworks`) or "Homework 2 / due Mon 7 Sep / 14/20 done" (`homeworkDoneCount` in `lib/homeworks.ts`: finished by the due date and by today, late never counts); `CLASS_HOMEWORK_STORY` (`data/homeworks.ts`) holds Homework 1 and 2 for all twenty, following each student's arc (HW1 17/20, HW2 14/20; Sam's his own story); `app/teacher/Classroom.tsx` lays Live and Past as one grid template (cards `minmax(0,1fr)`, 214 px column, 16 px gap), so every card is one width, the cell a plain div. |
| 307 | Sam's homework cells read their dates instead of a note: `HomeworkColumnPiece.submitted` (`lib/homeworks.ts`, the record's `finishedOn`, null while undone); `HomeworkColumn` (`app/student/StudentClassroom.tsx`) titles a missed cell "HW2 missing" and a completed one "HW1 completed", each with "due …" and, when handed in, "submitted …" beneath; `missedNote` and `MISSED_NOTE`/`MISSED_NOTE_CURRENT` removed (the carry-over itself unchanged); the Classroom's bottom padding matches the homework screen's so the last card clears SKIP TO | [architecture/307-hw-cell-dates.md](architecture/307-hw-cell-dates.md) |
| 314 | Where each student is, moment to moment: `lib/place.ts` gives every student's place at `now` (not started, confidence check, warm-up chat, warm-up on a skill at step 1–3, a question with a hint or practice at step 1–3, handed in, absent) and when that step began; `sessionPlace` reads Sam's session (the function tickets 312 and 313 extend), `classmateTimeline` the classmates' script on the stream's clock (a reload continues, a diagnostic chain stops it, a presenter skip is the end state), `placeRows` the lesson-order rows ticket 315 draws; `data/stream.ts` gains the classmates' warm-ups (Jordan non-monic; Mia fractions then non-monic; Oliver monic then non-monic; Tomas fractions; Amelia the discriminant), help on a real slip (Liam Q1 monic, Sofia Q2 non-monic, Harper Q3 expanding, Finn Q5 turning points) and hints (Noah two on Q3, Ethan Q4, Ruby Q9), all inside the existing times, no answer or hand-in moved | [architecture/314-student-place-model.md](architecture/314-student-place-model.md) |
| 311 | A line written into a blank step is checked against that step: `checkStep(step, line)` (`lib/stepCheck.ts`, pure, no knowledge of where the step came from) reads both lines with the app's one TeX grammar (`lib/texEval.ts`, now `tokenizeTex` + `expressionAt` → `TexNode`, `evalTex` evaluating the tree; typed shorthand through `toTex`) into a canonical form that forgets spacing, product signs, the order of factors, terms, sides and "or" cases, and keeps every step (nothing multiplied out or worked out); right when the forms are equal, else wrong with the first slip that rebuilds or explains the line (`SLIPS`: check wrong, minus not carried through, signs swapped in the pair, product right sum wrong and the reverse, sign lost solving for x / root sign / graph signs, divided by a not 2a, −b's minus dropped, part of the bottom dropped, factor missing, brackets don't expand back), unreadable when the grammar cannot read it; `warmupScript(p, slips)` = `padScript(steps, slips)` (`lib/warmup.ts`) writes a demo's wrong lines just before a step's own, and writes exactly the steps with none; no screen changes | [architecture/311-completion-line-check.md](architecture/311-completion-line-check.md) |
| 321 | The design tuner tunes the Classroom's box borders, problem set cards and homework cells apart, on the teacher's and Sam's Classroom: `--color-set-border` / `--color-hw-border` (`@theme`) and `--set-card-border-{width,style}` / `--hw-card-border-{width,style}` (`:root`) in `app/globals.css`, drawn by `@utility set-card-edge` / `hw-card-edge` as an inward outline over a clear 1px border so no width moves a box; `app/teacher/Classroom.tsx` and `app/student/StudentClassroom.tsx` colour the line with outline utilities (state colours kept, the sent cell's own dashed border untouched); `BORDER_KINDS` (`lib/designTokens.ts`) drives the Borders section in `components/DesignTuner.tsx` (width 0–4 px, solid/dashed/dotted, colour), whose colours leave the Colours list | [architecture/321-tuner-borders.md](architecture/321-tuner-borders.md) |
| 310 | The questions the worked example-problem pair runs on, data only: `data/pairs.ts` holds `QUESTION_PAIRS` (Q* worked and Q** finished, for each of Problem Set 6's ten questions: Q's stem, difficulty, answer form and kind of figure, as many steps with Q's skill tags step for step, one or two numbers changed; Q** with a hint for every point and the help chat's approaches where Q has a choice) and `COMPLETIONS` (one completion problem per practice skill, keyed as `PRACTICES`, between the practice problem as worked example and its follow-up done alone); the types beside `PracticeProblem` in `data/types.ts` (`WorkedQuestion`, `CompletionQuestion`, `QuestionPair`); `lib/pairs.ts` `blankSteps(steps, leaf)` is the one blank-line rule (the steps tagged with the named skill; all or none tagged → the last two, the last one of a two-step working), `pairFor`, `warmupSteps`; `components/Figure.tsx` gains the Q8* and Q8** graphs and `PARABOLAS`; no screen changes | [architecture/310-worked-example-pairs-data.md](architecture/310-worked-example-pairs-data.md) |

## Conventions

- **Server `page.tsx` reads params and hands an `init` object to a client screen.** Client
  components never read the URL themselves.
- **Components read data, never pages.** A chip needs only a subskill id.
- **Vocabulary lives in `data/types.ts`.** Rationale in `DECISION_LOG.md`.
- **`lib/` is for pure, testable logic** (escalation counter, group-phase computation).
- **Copy follows the rule in `specs/spec2.md`**: headlines two to four words, no
  explanatory sentence that doesn't change what the user does next, labels over sentences, at
  most one helper line per screen, no legends. The detective sentence is the one exception.
- **Student screens are designed at true iPad size** (1180×820) inside `IpadStage`; the stage
  scales, layouts never reflow.
