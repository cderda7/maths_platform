## Git
Overrides my global "no git" policy — for this project only, git is allowed: init, add, commit, status, diff, log. No push, rebase, force operations, or history rewrites.

Commit after each screen is completed and verified working — message names the screen (e.g. "class dashboard complete").
